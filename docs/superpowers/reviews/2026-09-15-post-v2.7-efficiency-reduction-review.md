# Repository and Context Reduction Audit

## Scope, baseline, and decision boundary

This is the Task 6 evidence review at `6d2c033e1ed133222061dcef7ca2343fb3711647`. It measures tracked repository facts and records recommendations only. It does not authorize or perform deletion, merging, archiving, derivation, worktree removal, pruning, branch changes, or any other mutation. Every `REMOVE`, `MERGE`, `DERIVE_INSTEAD_OF_STORE`, `ARCHIVE`, and `CLEAN_AFTER_WORK_UNIT` entry below is a recommendation, not execution authority. Any operation requires a separately governed Work Unit with then-current evidence.

Measurements use `git ls-files`, durable Git history, the consumer-projection manifest, `audit_projection_paths`, `build_consumer_inventory`, current release records and output sidecars, and the existing validation/continuity authorities. The tracked checkout contains 268 files and 2,165,119 bytes. A fact not established by those sources is explicitly `UNKNOWN`.

## Measured consumer projection

The manifest is 14,808 bytes and has 189 exact `paths` entries plus four prefix defaults:

| Prefix default | Classification | Current coverage |
| --- | --- | --- |
| `.gpt-codex/evidence/` | `MANAGEMENT_ONLY` | Management evidence paths |
| `docs/superpowers/` | `DEVELOPMENT_HISTORY` | The 49 pre-artifact tracked development-history files; this review is automatically covered when tracked |
| `.superpowers/sdd/` | `DEVELOPMENT_HISTORY` | All 6 tracked SDD reports |
| `releases/records/` | `RELEASE_METADATA` | All 20 tracked release records |

`audit_projection_paths` returned `unknown_paths=[]`, `missing_required_paths=[]`, and `invalid_classifications=[]`; `build_consumer_inventory` returned 86 paths. The only exact path beneath a default is `.superpowers/sdd/2026-09-12-v2.3.0-project-map-context-resume/.gitignore`, whose explicit `LOCAL_ONLY` classification intentionally overrides the SDD default. No redundant exact entry is otherwise measurable beneath the four defaults; the remaining exact entries retain explicit classifications, and exact entries take precedence over defaults.

Task1 (`89bdf42`) is the already-achieved measured reduction: the manifest changed from 26,980 to 14,808 bytes (12,172 bytes less) and from 316 to 189 exact entries (127 fewer), while safe prefix defaults rose from zero to four. Its current projection evidence is still zero unknown, zero missing required, and zero invalid classifications.

## Evidence table: required audit surfaces

| Candidate / Surface | Measured Evidence | Current Authority | Live Dependency | Recoverability | Durable History | Capability Impact | Classification | Reason | Next Action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Consumer projection manifest | 14,808 bytes; 189 exact entries; four defaults; audit has zero unknown/missing/invalid; inventory is 86 | `.gpt-codex/release/consumer-projection-manifest.json` and `consumer_projection.py` | Projection validator and consumer staging | Git reconstructable, but a replacement manifest is not proven | `89bdf42` records Task1 reduction | Removing or merging unproven rules could change consumer classification | `KEEP` | Existing authority and live validator dependency remain; no full reduction proof | Retain; evaluate individual exact rules only in a future Work Unit |
| `docs/superpowers/**` development history | 49 tracked files; 1,006,844 bytes: 24 plans / 646,242 bytes, 24 specs / 356,342 bytes, 1 prior review / 4,260 bytes before this audit | Prefix default classifies it `DEVELOPMENT_HISTORY`; accepted Design/Plan/review history is durable governance evidence | Consumer projection classification is live; direct runtime dependency is `UNKNOWN` | Git history exists; a capability-preserving replacement is not proven | Tracked Git history | Removing history would weaken recoverable design, plan, and review context | `KEEP` | Size alone is not reduction proof | Preserve the prefix classification and reassess only with candidate-specific replacement evidence |
| `.superpowers/sdd` | 6 tracked reports; 28,457 bytes | Prefix default classifies it `DEVELOPMENT_HISTORY`; reports retain implementation/release evidence | Runtime dependency not found in production paths; whether every report has no governance use is `UNKNOWN` | Git history exists; report-by-report reconstruction sufficiency is `UNKNOWN` | Tracked Git history | Capability preservation after removal is unproven | `KEEP` | Incomplete evidence fails closed | Retain all reports pending per-report evidence |
| Release metadata | 22 tracked files; 16,361 bytes; 20 records / 9,805 bytes; index names v2.7.0 as latest and archive policy `METADATA_ONLY` | `releases/INDEX.json`, current record, `validate_framework.py` | Framework validator requires the current release record; release tooling/tests reference records | Metadata is durable Git content, but replacement authority is not proven | Tracked release record history | Weakening records would break current-release validation and durable release authority | `KEEP` | Active validator and release authority | Preserve metadata-only policy; no reduction action |
| Current `dist` outputs | 3 tracked files; 119,426 bytes: v2.7.0 ZIP 118,946, SHA sidecar 107, release JSON 373 | v2.7.0 record and sidecar name the canonical artifact/hash; `dist` entries are `GENERATED_ARTIFACT` | Version and release-packaging tests reference current artifact and sidecar | Release tool can rebuild, but deletion-safe canonical handoff and after-capability proof are not established here | Current artifact is tracked and record points to its hash | Deleting it can impair present release/test capability | `KEEP` | Rebuildability alone is insufficient | Keep current outputs; consider only after a governed canonical-artifact transition |
| Current Task6 worktree | Clean at audit start; HEAD `6d2c033`; active Task6 Work Unit is true | Current Task6 authority | Active execution | Not applicable while active | Durable SHA exists | Removing active workspace would interrupt capability | `KEEP` | `active_work_unit=True` forces Task5 decision to `KEEP` | Keep until a later Work Unit supplies all four facts |
| Registered legacy worktrees | `git worktree list --porcelain` reports 39 registrations, including one `prunable` registration | Git worktree metadata | Review/integration sufficiency and dirty-only-data facts are `UNKNOWN` for each candidate | Repository refs exist for listed worktrees; individual recovery facts are incomplete | Listed SHAs/branches are durable where present | Per-worktree capability preservation is unverified | `KEEP` | Any unknown Task5 input returns `KEEP` | Collect current per-worktree facts in a separate governed Work Unit |
| Future completed worktree with all Task5 facts | Only the exact tuple `False, True, True, False` returns `CLEAN` from `evaluate_worktree_cleanup` | Task5 `git_continuity.evaluate_worktree_cleanup` | No operation is invoked by the function | Only after a durable Git authority is true | Required by the input tuple | Bounded decision preserves fail-closed handling | `CLEAN_AFTER_WORK_UNIT` | This is a conditional future recommendation, not a finding about any current worktree | A separate Work Unit must verify facts, inspect state, and use native Git without force removal |

## Prior Task-8 KEEP candidates

The prior `docs/superpowers/reviews/2026-09-15-minimal-harness-reduction-review.md` remains authoritative. It records nine candidates as `KEEP`, no removal candidate, and requires a separate Work Unit for any reduction. This audit found no new complete replacement evidence; prior KEEP authority is therefore preserved.

| Candidate | Measured / durable evidence | Current authority and live dependency | Recoverability / capability result | Classification | Next Action |
| --- | --- | --- | --- | --- | --- |
| Work Unit | Prior review cites the work-unit schema and project-separation test | Durable project-scoped authorization and Result correlation | No capability-preserving replacement proven | `KEEP` | Preserve prior authority |
| Execution slots | Prior review cites `continuity_resume.py` validation and tests | One-active-work lifecycle and fail-closed transitions | No replacement proven | `KEEP` | Preserve prior authority |
| Resume persistence | Prior review cites `continuity_resume.py` and tests | Verified checkpoints and cold recovery | No replacement proven | `KEEP` | Preserve prior authority |
| Project Map | Prior review cites `project_navigation.py` and tests | Validated routing and stale-context recovery | No replacement proven | `KEEP` | Preserve prior authority |
| Telemetry runtime | Prior review cites `execution_telemetry.py` and tests | Bounded, non-authoritative observations | No replacement proven | `KEEP` | Preserve prior authority |
| Instruction Envelope | Prior review distinguishes derived compact task presentation from authority | Canonical issuer/executor/action/correlation contract | Derived view is not a replacement | `KEEP` | Preserve prior authority |
| Result Envelope / return | Prior review distinguishes compact return from authority | Canonical Result/evidence/return contract | Derived view is not a replacement | `KEEP` | Preserve prior authority |
| Repeated schemas / validators | Prior review cites layered validation and tests | Distinct fail-closed boundaries | Duplicate responsibility is not proven | `KEEP` | Preserve prior authority |
| Module boundaries | Prior review cites seven-module registry and routing tests | Explicit responsibility ownership and conflict rejection | Mergeable boundary is not proven | `KEEP` | Preserve prior authority |

## Reduction accounting

### Already achieved reduction

- Task1 has the measured 12,172-byte manifest reduction and 127-entry exact-rule reduction stated above, without any consumer-projection loss.
- Task4 (`d99a56e`) locks retained verification evidence and prevents transport-only evidence loss from triggering verification reruns; it is a bounded rerun-cost reduction, not a new result authority.
- Task5 (`6d2c033`) adds the pure four-fact worktree decision. It adds bounded decision coverage only; it did not clean any worktree.

### Future-growth reduction

- The three development/release prefix defaults mean that new files under `docs/superpowers/`, `.superpowers/sdd/`, and `releases/records/` do not need individual exact manifest entries. They remain classified, while unknown paths outside safe prefixes still fail closed.
- Task2 publication-preflight and immutable-artifact-handoff evidence remains an existing bounded gate; it is not counted as a physical repository reduction.

### Proposed future reduction

No current repository object has a complete proof for `REMOVE`, `MERGE`, `DERIVE_INSTEAD_OF_STORE`, or `ARCHIVE`. The only proposed future reduction category is `CLEAN_AFTER_WORK_UNIT`, and only for an individual worktree that later proves all Task5 inputs as `False, True, True, False`. It is not an authorization and has not been executed.

## Fail-closed conclusion

The reduction classifications used by this audit are limited to `KEEP` and the one conditional `CLEAN_AFTER_WORK_UNIT` recommendation. No candidate is classified `REMOVE`, `MERGE`, `DERIVE_INSTEAD_OF_STORE`, or `ARCHIVE`, because at least one required condition is incomplete, unknown, or unproven. In every such case, `AFTER_CAPABILITIES >= BEFORE_CAPABILITIES` cannot be demonstrated, so the audit retains `KEEP`.

Task2 publication/preflight evidence, Task4 verification/evidence-transport closure, Task5 cleanup decision, current Framework/Project validation authority, and prior Task-8 KEEP decisions were reused as evidence only. This document creates no inventory subsystem, cleanup tool, result authority, or execution permission.
