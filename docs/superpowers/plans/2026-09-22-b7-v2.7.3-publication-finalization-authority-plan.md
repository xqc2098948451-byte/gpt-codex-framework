# B7 v2.7.3 Publication Finalization Authority Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. This Plan candidate grants no execution authority.

**Goal:** Turn independently verified v2.7.3 publication into durable State/Evidence finalization and `REMOTE_ACTIVE` without altering the already-published release.

**Architecture:** Reuse current Result, Evidence, and State finalization contracts and the historical three-path fix2 pattern semantically. Create exactly one dedicated revision-16 finalization Work Unit through a one-time `CREATE_ONLY` `USER_LOCAL` bootstrap; execute one native three-path finalization; then integrate exact reviewed finalization bytes through bounded `USER_LOCAL` transport.

**Tech Stack:** Existing JSON schemas, instruction/result contracts, `publication_contract.py`, `validate_framework.py`, `validate_project.py`, Git/GitHub continuity.

**Spec:** `ebe14209d7990bcab177079ad2cba9fee97abb0b:docs/superpowers/specs/2026-09-22-b7-v2.7.3-publication-finalization-authority-design.md`

## Global Constraints

- `W` is immutable: `fdcda12c6b0b94ac919d507ba8172ccce2f2dea2`.
- State basis revision is `16`; durable final State revision is `17`.
- Finalization mutation scope is exactly three paths: State, one Evidence, and one Result.
- No source, package, publication, tag, Release, asset, `VERSION`, release record, or index mutation is permitted.
- No schema change, Kernel change, Group C implementation, Plugin, or automatic evolution is permitted.
- `CODEX_IMPLEMENTER` never has `COMMIT` or `PUSH` authority.
- Bootstrap is dedicated, one-time, `CREATE_ONLY`, and `USER_LOCAL`-executed.
- Publication is already verified and must not be repeated; later observations only confirm its immutability.

## Review Focus

1. A changed `main` or State revision after a required fresh observation stops the lifecycle with `RECONCILIATION_REQUIRED`.
2. The bootstrap creates exactly one Work Unit path and can never be reused as a factory.
3. The native execution can mutate only the three finalization paths and has no transport authority.
4. Evidence, Result, and State retain exact existing contract semantics and bind only independently observed publication facts.
5. `REMOTE_ACTIVE` opens only after independent verification of main containing the exact three-path finalization commit; C0 remains read-only.

---

## Phase 1 — Plan integration and post-Plan base freeze

**Files:**
- Modify later: only this accepted Plan path during its own candidate transport.
- Read: accepted Design, `origin/main`, `.gpt-codex/STATE.json`.

- [ ] Obtain independent Plan review, GPT adjudication, and exact USER Plan acceptance for the exact Plan commit.
- [ ] Integrate that exact accepted Plan commit to `main` only by separately authorized `USER_LOCAL` transport, then independently verify remote `main`.
- [ ] Perform a new fresh observation only after that integration and freeze `FINALIZATION_AUTHORITY_BASE_SHA` as that exact `origin/main` SHA and `STATE_REVISION` as `16`.
- [ ] Reject automatic rebase, latest-main substitution, or every subsequent main change with `RECONCILIATION_REQUIRED`.

## Phase 2 — Freeze canonical finalization Work Unit bytes

**Files:**
- Create later: `.gpt-codex/work-units/framework-v2.7.3-publication-finalization-001.json`.

`ACCEPTED_PLAN_SHA` means the exact USER-accepted and durably integrated Plan
commit observed from `origin/main` after Plan integration. It is a formally
defined Phase-1 runtime binding, not a free placeholder. A literal
`ACCEPTED_PLAN_SHA` is forbidden in final Work Unit bytes: replace it with the
observed 40-hex SHA before serialization, then construct and serialize this
one complete canonical Work Unit object exactly once, in this field order:

```json
{
  "kernel_version": "2.0.0",
  "schema_version": 1,
  "project_id": "PRJ-FRAMEWORK-MANAGEMENT",
  "work_unit_id": "framework-v2.7.3-publication-finalization-001",
  "goal": "Finalize independently verified v2.7.3 remote publication facts into durable repository State/Evidence without changing publication or authorizing Group C implementation.",
  "scope": {"owned_paths":[".gpt-codex/STATE.json", ".gpt-codex/evidence/V2.7.3-REMOTE-PUBLICATION-VERIFICATION.json", ".gpt-codex/evidence/results/RESULT-V2.7.3-PUBLICATION.json"],"excluded_paths":[]},
  "acceptance": {"purpose":"V2.7.3_PUBLICATION_STATE_FINALIZATION","framework_version":"2.7.3","source_candidate_sha":"d9dd439a6d6ca944a6f07ed1aa0b386b0ac0a0b7","source_candidate_content_set_sha256":"7862243dc275f3f27d600717d5e2bdc3a1373f9e49eb9bc80346b23df440efe6","W":"fdcda12c6b0b94ac919d507ba8172ccce2f2dea2","remote_main":"fdcda12c6b0b94ac919d507ba8172ccce2f2dea2","annotated_tag_object":"66dcf92e418bf402183ab33ad545e632cff12e62","tag":"v2.7.3","tag_target":"fdcda12c6b0b94ac919d507ba8172ccce2f2dea2","github_release_id":393538102,"github_release_url":"https://github.com/xqc2098948451-byte/gpt-codex-framework/releases/tag/v2.7.3","release_published":true,"asset_id":580868060,"asset_name":"gpt-codex-framework-v2.7.3-bootstrap.zip","asset_sha256":"8da1be98f9d7ea18fb3008685331956dfcb2232ac1ccfdf9085dd466a793a39d","asset_size_bytes":139774,"remote_version":"2.7.3","release_record_path":"releases/records/v2.7.3.json","release_record_blob_sha":"2d8c222b181e4429a21d82d348be2d67f98264cb","release_record_sha256":"a50e4580d046b5b0d1dc608adb244f31028b54f64e72a8e8d7d852ac3bbce8d0","release_index_path":"releases/INDEX.json","release_index_blob_sha":"09639abc4ccec9fea84f52d5e2f8862a37d21300","release_index_sha256":"31f715a306ad3f10817ba503d20d0f3648d3c68f5239962a35202027d0212522"},
  "selected_extensions": {"skills":["github-project-continuity"],"guardrails":["universal-safety","cross-project-context-binding","github-repository-binding"],"fitness":[]},
  "permissions": {"authorized_actions":["READ","TEST","VALIDATE","REPORT","MUTATE_APPROVED_SCOPE"],"forbidden_actions":["COMMIT","PUSH","PUBLISH","AUTHORIZE","SCOPE_EXPANSION"]},
  "state": "AUTHORIZED",
  "basis_state_revision": 16,
  "evidence_refs": [],
  "artifact_refs": {"design":{"path":"docs/superpowers/specs/2026-09-22-b7-v2.7.3-publication-finalization-authority-design.md","sha":"ebe14209d7990bcab177079ad2cba9fee97abb0b"},"plan":{"path":"docs/superpowers/plans/2026-09-22-b7-v2.7.3-publication-finalization-authority-plan.md","sha":"<resolved accepted Plan SHA>"}}
}
```

The bracketed explanatory value is replaced before serialization, never copied
into final bytes. Only independently `TOOL_OBSERVED` verified publication facts
may be recorded. Canonically serialize via `json.dumps(..., ensure_ascii=False,
indent=2)`, UTF-8, LF line endings, exactly one final LF, no BOM, and no
trailing spaces. Freeze `FINALIZATION_WORK_UNIT_BYTES`,
`FINALIZATION_WORK_UNIT_BYTE_COUNT`, `FINALIZATION_WORK_UNIT_SHA256`, and
`FINALIZATION_WORK_UNIT_GIT_BLOB_SHA1`; byte freeze performs no repository
mutation. The Work Unit cannot authorize its own creation, COMMIT, PUSH,
publication, Group C implementation, Plugin, or scope expansion, and becomes
stale after State revision advances to `17`.

## Phase 3 — Dedicated one-time bootstrap

**Files:**
- Create later: only `.gpt-codex/work-units/framework-v2.7.3-publication-finalization-001.json`.

The dedicated bootstrap is `framework-v2.7.3-publication-finalization-work-unit-bootstrap-001` at `refs/tags/bootstrap/framework-v2.7.3-publication-finalization-work-unit-001`, with `executor_role = USER_LOCAL`, `operation = CREATE_ONLY`, `one_time = true`, and exactly the one Work Unit mutation path above.

Before bootstrap, resolve the earlier defined runtime bindings
`FINALIZATION_AUTHORITY_BASE_SHA`, `ACCEPTED_PLAN_SHA`, and
`FINALIZATION_WORK_UNIT_SHA256` to concrete values. They are not free
placeholders. Construct one complete canonical JSON payload, in this exact key
order (the three bracketed explanatory values are resolved before serialization
and are forbidden in final bytes):

```json
{"bootstrap_id":"framework-v2.7.3-publication-finalization-work-unit-bootstrap-001","repository_id":1366213495,"repository_full_name":"xqc2098948451-byte/gpt-codex-framework","default_branch":"main","expected_base_sha":"<resolved FINALIZATION_AUTHORITY_BASE_SHA>","expected_state_revision":16,"accepted_design_ref":"ebe14209d7990bcab177079ad2cba9fee97abb0b:docs/superpowers/specs/2026-09-22-b7-v2.7.3-publication-finalization-authority-design.md","accepted_plan_ref":"<resolved ACCEPTED_PLAN_SHA>:docs/superpowers/plans/2026-09-22-b7-v2.7.3-publication-finalization-authority-plan.md","finalization_work_unit_id":"framework-v2.7.3-publication-finalization-001","finalization_work_unit_path":".gpt-codex/work-units/framework-v2.7.3-publication-finalization-001.json","finalization_work_unit_canonical_bytes_sha256":"<resolved FINALIZATION_WORK_UNIT_SHA256>","framework_version":"2.7.3","source_candidate_sha":"d9dd439a6d6ca944a6f07ed1aa0b386b0ac0a0b7","source_candidate_content_set_sha256":"7862243dc275f3f27d600717d5e2bdc3a1373f9e49eb9bc80346b23df440efe6","W":"fdcda12c6b0b94ac919d507ba8172ccce2f2dea2","remote_main":"fdcda12c6b0b94ac919d507ba8172ccce2f2dea2","annotated_tag_object_sha":"66dcf92e418bf402183ab33ad545e632cff12e62","tag":"v2.7.3","tag_target_sha":"fdcda12c6b0b94ac919d507ba8172ccce2f2dea2","github_release_id":393538102,"github_release_url":"https://github.com/xqc2098948451-byte/gpt-codex-framework/releases/tag/v2.7.3","release_published":true,"asset_id":580868060,"asset_name":"gpt-codex-framework-v2.7.3-bootstrap.zip","asset_sha256":"8da1be98f9d7ea18fb3008685331956dfcb2232ac1ccfdf9085dd466a793a39d","asset_size_bytes":139774,"remote_version":"2.7.3","release_record_path":"releases/records/v2.7.3.json","release_record_blob_sha":"2d8c222b181e4429a21d82d348be2d67f98264cb","release_record_sha256":"a50e4580d046b5b0d1dc608adb244f31028b54f64e72a8e8d7d852ac3bbce8d0","release_index_path":"releases/INDEX.json","release_index_blob_sha":"09639abc4ccec9fea84f52d5e2f8862a37d21300","release_index_sha256":"31f715a306ad3f10817ba503d20d0f3648d3c68f5239962a35202027d0212522","mutation_scope":[".gpt-codex/work-units/framework-v2.7.3-publication-finalization-001.json"],"operation":"CREATE_ONLY","executor_role":"USER_LOCAL","one_time":true}
```

Serialize this object exactly once with `json.dumps(..., ensure_ascii=False,
indent=2)`, UTF-8, LF only, one final LF, no BOM, and no trailing spaces.
Freeze `BOOTSTRAP_PAYLOAD_BYTES`, `BOOTSTRAP_PAYLOAD_BYTE_COUNT`, and
`BOOTSTRAP_PAYLOAD_SHA256`. The annotated bootstrap tag message equals those
exact frozen payload bytes; its target commit equals
`FINALIZATION_AUTHORITY_BASE_SHA`.

```text
canonical bytes freeze
-> independent bootstrap PRE_EXECUTION review
-> exact USER bootstrap payload approval
-> annotated bootstrap tag targeting exact frozen base
-> byte-exact Work Unit materialization
-> exact one-path Work Unit candidate
-> independent remote verification
-> exact USER_LOCAL main fast-forward
-> independent main verification
-> BOOTSTRAP = TERMINATED
-> reuse forbidden
```

No release, packaging, historical fix2, or generic bootstrap/factory is reusable authority.

### Design §9 preservation invariant

Before bootstrap, before native execution, before candidate transport, and
after final main integration, independently require unchanged: `S`
`d9dd439a6d6ca944a6f07ed1aa0b386b0ac0a0b7`; `S_CONTENT_SET_SHA256`
`7862243dc275f3f27d600717d5e2bdc3a1373f9e49eb9bc80346b23df440efe6`; `W`
`fdcda12c6b0b94ac919d507ba8172ccce2f2dea2`; annotated tag object
`66dcf92e418bf402183ab33ad545e632cff12e62`; GitHub Release `393538102`;
asset `580868060` with its accepted bytes/digest/size; release record
`releases/records/v2.7.3.json` with blob
`2d8c222b181e4429a21d82d348be2d67f98264cb` and SHA-256
`a50e4580d046b5b0d1dc608adb244f31028b54f64e72a8e8d7d852ac3bbce8d0`;
release index `releases/INDEX.json` with blob
`09639abc4ccec9fea84f52d5e2f8862a37d21300` and SHA-256
`31f715a306ad3f10817ba503d20d0f3648d3c68f5239962a35202027d0212522`; and
publication packaging files byte-identical to `W`. Any difference is
`RECONCILIATION_REQUIRED`.

## Phase 4 — Native finalization PRE_EXECUTION

**Files:**
- Modify later: only the three paths in Phase 2.

After Work Unit integration and bootstrap termination, first run the Design §9
preservation invariant. Then freshly require `origin/main` equals the exact
Work Unit integration SHA, State revision equals `16`, all published facts are
unchanged, and the Work Unit hash/ref is exact. Construct the current
production-builder `EXECUTION_INSTRUCTION` with `executor_role =
CODEX_IMPLEMENTER`, scope_paths equal to the three paths, `completion_gate =
GPT_DECISION`, the Phase 2 authorized actions, and the Phase 2 forbidden
actions. A fresh independent `CODEX_REVIEWER` PRE_EXECUTION review must approve
all bindings; any mismatch stops before mutation.

## Phase 5 — Exact Evidence mapping

**Files:**
- Create later: `.gpt-codex/evidence/V2.7.3-REMOTE-PUBLICATION-VERIFICATION.json`.

Inspect `.gpt-codex/schemas/evidence.schema.json` and current publication evidence before writing. The supported concrete mapping is:

```json
{
  "kernel_version": "2.0.0",
  "schema_version": 1,
  "evidence_id": "V2.7.3-REMOTE-PUBLICATION-VERIFICATION",
  "project_id": "PRJ-FRAMEWORK-MANAGEMENT",
  "source": "TOOL_OBSERVED",
  "subject": "V2.7.3_CONFIRMED_REMOTE_PUBLICATION",
  "state_revision": 16,
  "result": "VERIFIED",
  "repository_identity": "xqc2098948451-byte/gpt-codex-framework",
  "repository_remote_url": "https://github.com/xqc2098948451-byte/gpt-codex-framework.git",
  "current_remote_ref": "refs/heads/main",
  "remote_main_sha": "fdcda12c6b0b94ac919d507ba8172ccce2f2dea2",
  "work_sha": "fdcda12c6b0b94ac919d507ba8172ccce2f2dea2",
  "tag": "v2.7.3",
  "annotated_tag_object_sha": "66dcf92e418bf402183ab33ad545e632cff12e62",
  "tag_target_sha": "fdcda12c6b0b94ac919d507ba8172ccce2f2dea2",
  "release": {"id": 393538102, "version": "v2.7.3", "published": true, "url": "https://github.com/xqc2098948451-byte/gpt-codex-framework/releases/tag/v2.7.3"},
  "remote_version": "2.7.3",
  "artifact": {"id": 580868060, "name": "gpt-codex-framework-v2.7.3-bootstrap.zip", "sha256": "8da1be98f9d7ea18fb3008685331956dfcb2232ac1ccfdf9085dd466a793a39d", "size_bytes": 139774},
  "verification": {"repository_identity": "PASS", "remote_main": "PASS", "annotated_tag": "PASS", "release": "PASS", "remote_version": "PASS", "artifact_sha256": "PASS", "artifact_size": "PASS", "release_record_consistency": "PASS", "source_ancestry": "PASS", "consumer_validation": "PASS", "framework_validation": "PASS", "project_validation": "PASS"}
}
```

No publication write occurs. If the current schema cannot validate this concrete supported shape, stop with `AMENDMENT_REQUIRED`.

## Phase 6 — Exact Result mapping

**Files:**
- Create later: `.gpt-codex/evidence/results/RESULT-V2.7.3-PUBLICATION.json`.

Inspect result-envelope schema, `result_return.py`, `publication_contract.py`, and the v2.7.2+fix.2 Result. The new Result must use those existing contracts and the following concrete values: kernel/schema/project identity as above; `work_unit_id = framework-v2.7.3-publication-finalization-001`; `extension.kind = SKILL`, `extension.id = github-project-continuity`, `extension.version = 1.0.0`; `status = PASS`; `result_message_type = IMPLEMENTATION_RESULT`; `responder_role = CODEX_IMPLEMENTER`; `return_role = GPT_ORCHESTRATOR`; `framework_version = 2.7.3`; source repository ID/full name `1366213495`/`xqc2098948451-byte/gpt-codex-framework`; `current_remote_ref = refs/heads/main`; `local_head_sha`, `remote_head_sha`, and `verified_baseline_sha` all equal `fdcda12c6b0b94ac919d507ba8172ccce2f2dea2`; `sync_status = SYNCED`; `push_status = SUCCEEDED`; `remote_verification = VERIFIED`; `publication_authority = CONFIRMED_PUBLICATION`; `state_revision = 17`; and `completion_gate = GPT_DECISION`.

`changed`, `completion_evidence.intended_scope`, and `completion_evidence.executed_scope` are the exact three paths in Phase 2; `evidence_refs` is exactly `[".gpt-codex/evidence/V2.7.3-REMOTE-PUBLICATION-VERIFICATION.json"]`; `deviations` and `blockers` are `[]`; the `verify` array records the exact remote main, tag target, remote VERSION, asset SHA-256, and asset size; and `completion_evidence` is `COMPLETED`, process completed true, exit code zero, test file counts zero and equal, failure/error counts zero, and equal validator arrays `["evidence-schema", "result-envelope-contract", "result-authority", "completion-evidence", "state-schema", "state-authority", "git-diff-check"]`.

`response_to_instruction_id` must be the exact UUID emitted by the production builder and `source_project_context_id`/`source_project_name` must equal the freshly bound production instruction values. No field may claim that the future finalization commit caused publication; publication predates finalization.

## Phase 7 — State revision 16 to 17

**Files:**
- Modify later: `.gpt-codex/STATE.json`.

Inspect current State, state schema, `validate_project.py`, `publication_contract.py`, and historical transitions. Preserve every unrelated current field, set `revision = 17`, retain `state = AUTHORIZED` unless the then-current validator requires an existing transition, append the Evidence and Result paths from Phases 5–6, and set continuity to:

```json
{"current_remote_ref":"refs/heads/main","latest_verified_remote_sha":"fdcda12c6b0b94ac919d507ba8172ccce2f2dea2","latest_synced_state_revision":17,"last_verified_result_ref":".gpt-codex/evidence/results/RESULT-V2.7.3-PUBLICATION.json","sync_status":"SYNCED"}
```

The exact valid next action is `GROUP_C_PRE_EXECUTION_REOBSERVATION_REQUIRED`: the current State schema accepts nonempty `next_action` values and no validator reserves this value. It denotes only read-only C0 re-observation; it does not activate a Group C Work Unit or C1/C2 implementation authority.

## Phase 8 — Validation

Before POST_EXECUTION review, require zero failures from:

```text
python -B .gpt-codex/scripts/validate_consumer_projection.py --root .
python -B .gpt-codex/scripts/validate_framework.py
python -B .gpt-codex/scripts/validate_project.py .
python -B .gpt-codex/tests/test_publication_authority.py
python -B .gpt-codex/tests/test_remote_verification.py
python -B -m unittest discover -s .gpt-codex/tests -p "test_*.py"
git diff --check
```

Also directly validate Evidence against its schema, Result against result-envelope and publication authority/completion evidence contracts, and State against state schema/state authority. Any schema expansion need is `AMENDMENT_REQUIRED`.

## Phase 9 — Content lock and POST_EXECUTION review

The actual Git mutation union must equal exactly the three paths. For each final file compute raw SHA-256; sort paths using `sorted(paths, key=str.casefold)`; canonicalize each line as `<path><TAB><sha256><LF>`; then SHA-256 those bytes as `FINALIZATION_CANDIDATE_CONTENT_SET_SHA256`.

A fresh independent `CODEX_REVIEWER` POST_EXECUTION review must re-observe remote publication, verify the three paths, recompute all per-path and aggregate hashes, confirm State `16 -> 17`, validate Evidence/Result contracts, and reject any publication, Group C, or fourth-path mutation. The reviewer aggregate must equal the candidate aggregate exactly.

## Phase 10 — USER acceptance and USER_LOCAL integration

Present the aggregate and three file hashes, pre-finalization base, State revision `17`, and exact W/tag/Release/asset facts. One USER response may provide `USER_LOCAL_FINALIZATION_COMMIT_AUTHORIZATION=APPROVE`, `USER_LOCAL_FINALIZATION_CANDIDATE_PUSH_AUTHORIZATION=APPROVE`, and `USER_LOCAL_FINALIZATION_MAIN_FAST_FORWARD_AUTHORIZATION=APPROVE_IF_REMOTE_VERIFICATION_PASSES`. Then close `CODEX_IMPLEMENTER_EXECUTION_WINDOW`.

Before candidate transport, and again after final main integration, run the
Design §9 preservation invariant. Only `USER_LOCAL` may stage the three paths,
verify the index content lock, create one commit with the accepted
pre-finalization base as parent, push non-force only
`governance/b7-v2.7.3-publication-finalization-candidate-001`, independently
verify it, and conditionally non-force fast-forward it to main. No edit,
amend, rebase, squash, second commit, or extra path is allowed.

## Phase 11 — `REMOTE_ACTIVE` and C0 gate

Only after remote main contains the exact finalization commit and exact State revision 17/Evidence/Result bytes are independently verified does v2.7.3 become `REMOTE_ACTIVE`. Then and only then opens `C0 = READ_ONLY_FRESH_REOBSERVATION`. C1+, v2.7.4 mutation, Plugin, and automatic evolution remain unauthorized.

## Failure Rules

| Condition | Outcome |
| --- | --- |
| main/base changed; State revision not 16; published W/tag/Release/asset mismatch; bootstrap tag/path exists; publication write required; State revision 17 is not exact | `RECONCILIATION_REQUIRED` |
| Work Unit hash/scope mismatch; CODEX_IMPLEMENTER COMMIT/PUSH | `ROLE_AUTHORITY_CONFLICT / FAIL` |
| fourth finalization path | `FAIL` |
| schema expansion needed | `AMENDMENT_REQUIRED` |
| POST-review content-lock mismatch | `REVIEW_FINDING / STOP` |
| remote candidate mismatch | `STOP`, without main integration |
| C1+ attempted before `REMOTE_ACTIVE` | `BLOCKED` |

## Candidate self-review and transport

Before Plan candidate transport, map every Design section to the phases above; verify no unresolved markers, no WORK_UNIT self-creation, no CODEX commit/push, no publication authority, no Group C mutation, and only one Plan path changed. Run `git diff --check`, `python -B .gpt-codex/scripts/validate_framework.py`, and `python -B .gpt-codex/scripts/validate_project.py .`.

Commit exactly this path with `docs: plan b7 v2.7.3 publication finalization authority`, push only `governance/b7-v2.7.3-publication-finalization-authority-plan`, and leave main at `ebe14209d7990bcab177079ad2cba9fee97abb0b`. Do not create a Work Unit, bootstrap, State/Evidence, or publication artifact.
