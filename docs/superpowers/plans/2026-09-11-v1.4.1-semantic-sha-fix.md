# GPT–Codex Framework v1.4.1 Semantic SHA Fix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remove Git/governance SHA self-reference ambiguity while preserving v1.4 task-scoped Git automation.

**Architecture:** Keep Git as the factual source of repository history while governance artifacts record semantic bindings and observations. Separate implementation identity from repository HEAD and classify change surfaces so governance-only metadata commits do not invalidate implementation review.

**Tech Stack:** Markdown governance framework, Git semantics, ZIP distribution.

**Spec:** `docs/superpowers/specs/2026-09-11-v1.4.1-semantic-sha-fix-design.md`

## Global Constraints

- Universal framework only; no downstream project migration.
- Preserve Bootstrap Validation, Compact Handoff, Context Continuity, Action Routing, Handoff Manifest, and Controlled Git Lifecycle.
- Release version is exactly `1.4.1`.
- Universal framework distribution must preserve its own `docs/superpowers/specs/` and `docs/superpowers/plans/` history; downstream migration packaging is a separate concern.

---

### Task 1: Patch semantic SHA rules

**Files:** `.gpt-codex/FRAMEWORK.md`, `AGENTS.md`, `.gpt-codex/BOOTSTRAP_PROMPT.md`, `.gpt-codex/README.md`

- [x] Replace HEAD-equality assumptions with semantic implementation SHA vs observed repository HEAD.
- [x] Add change-surface classification and review invalidation rules.
- [x] Make Git-specific reconciliation precedence explicit.

### Task 2: Patch governance templates

**Files:** `.gpt-codex/templates/STATE.template.md`, `HANDOFF.template.md`, `TASK.template.md`, `REVIEW.template.md`, `CODEX_EXECUTION_PACKET.template.md`, `GIT_INTEGRATION.template.md`, `PROJECT.template.md`

- [x] Add stable semantic SHA fields and `State Basis`.
- [x] Remove self-referential HEAD freshness requirements.
- [x] Bind review/integration to implementation SHA while allowing governance-only descendants.

### Task 3: Release hygiene

**Files:** `.gpt-codex/CHANGELOG.md`, distribution tree

- [x] Add v1.4.1 changelog.
- [x] Update version markers.
- [x] Preserve framework-development specs/plans in the universal framework distribution.
- [x] Run rule consistency, stale-term, Markdown presence, ZIP integrity, and SHA-256 checks.
