# Post-v2.7 Minimal Closed-Loop Efficiency Optimization Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use test-driven-development for every behavior change, verification-before-completion before completion claims, and requesting-code-review at milestone/risk gates.

**Goal:** Reduce current and future management cost while preserving every required Framework capability and the existing closed loop.

**Architecture:** Extend existing authorities only: projection classification remains in `consumer_projection.py`, publication checks remain in the publication/release contract, verification remains a skill/Evidence contract, and cleanup decisions remain in `git_continuity.py`. The implementation is sequential: classification first, bounded preflight and evidence rules next, then a decision-only lifecycle rule, a review-only audit, and an integrated preservation gate.

**Tech Stack:** Python 3 standard library, `unittest`, JSON manifests, Git CLI, Markdown skills and maintenance records.

**Design / Spec Authority:** `6c6645cdab0d05271d0c8bf7de9f18c5b33997d9:docs/superpowers/specs/2026-09-15-post-v2.7-minimal-closed-loop-efficiency-optimization-design.md`.

## Global Constraints

- 保留能力，压缩结构；保留闭环，删除重复。
- 真实问题必须闭环到根因；已经由现有能力解决的问题不再新增机制。
- `AFTER_CAPABILITIES >= BEFORE_CAPABILITIES`; Framework module count remains `7`; no eighth module.
- No Kernel version, schema version, or Framework `VERSION` change; a discovered unavoidable schema compatibility issue requires Design and Plan revision before action.
- No release/publication, Git-history rewrite, force push, automatic P0 removal, deletion/merge/derivation without evidence, tracing platform, worktree database, second release subsystem, or private chain-of-thought storage.
- Prior Task-8 nine-candidate `KEEP` decisions remain authoritative unless new evidence proves otherwise.
- `REFERENCE_FIRST`; contamination scanning stays fail-closed; `CONSUMER_REQUIRED` stays explicit exact authority and no prefix can include a consumer file.
- This Plan contains no Context Lifecycle or Handoff Completeness requirement and does not generalize the isolated attachment observation.

## Baseline Measurements

Before Task 1, record from `6c6645cdab0d05271d0c8bf7de9f18c5b33997d9`: manifest byte length; number of `paths` entries; number of exact `DEVELOPMENT_HISTORY` and `MANAGEMENT_ONLY` entries covered by the four proposed prefixes; and the sorted output of `build_consumer_inventory(root, manifest)`. Preserve these values as Task 6 evidence. Before Tasks 2–4, record the existing release blocker checks, verification rerun rule coverage, and cleanup-decision test count. Unknown metrics are recorded as `UNKNOWN`, never fabricated.

## Dependency Order

`Task 1 -> Task 2 -> Task 3 -> Task 4 -> Task 5 -> Task 6`. Task 1 makes later plan/review history automatically classifiable. This sequence is an authority dependency, not authorization to parallelize Codex work.

## Task 1: Safe Exclusion Prefix Defaults and Manifest Compression

**GOAL:** Add deterministic, non-consumer prefix defaults and remove only redundant exact exclusions.

**SCOPE:** Extend `.gpt-codex/scripts/consumer_projection.py`, `.gpt-codex/release/consumer-projection-manifest.json`, and `.gpt-codex/tests/test_consumer_projection.py`.

**CONSTRAINTS:** Prefixes are normalized deterministic directory prefixes only: `.gpt-codex/evidence/ -> MANAGEMENT_ONLY`, `docs/superpowers/ -> DEVELOPMENT_HISTORY`, `.superpowers/sdd/ -> DEVELOPMENT_HISTORY`, and `releases/records/ -> RELEASE_METADATA`. Reject glob/regex syntax and `CONSUMER_REQUIRED` defaults. Exact `paths` wins; otherwise select the longest matching normalized prefix. Unknown remains failing; missing consumer paths remain exact-path based; inventory and contamination scanning remain unchanged.

**DONE:** The current audit reports unknown `0`, missing `0`, invalid `0`; the consumer inventory equals the recorded baseline; and manifest bytes or redundant exact non-consumer entries are measurably reduced.

**Files and interfaces:** Modify `_normalise_manifest_key(value: object) -> str | None`, `audit_projection_paths(root: Path, manifest: Mapping[str, object]) -> dict[str, list[str]]`, `build_consumer_inventory(...)`, and `stage_consumer_projection(...)`. Add `_normalise_manifest_prefix(value: object) -> str | None` and `_resolve_projection_classification(path: str, exact_paths: Mapping[str, str], prefix_defaults: Mapping[str, str]) -> str | None`. Add a `prefix_defaults` manifest object; retain `paths` for exact authority only. Extend existing `ConsumerProjectionTests` and `minimal_manifest()` fixtures.

- [ ] Write RED tests for evidence and `docs/superpowers/` default classification, exact-over-prefix precedence, longest nested prefix, rejected consumer prefix, rejected wildcard prefix, unmatched root unknown failure, and unchanged consumer inventory when only a prefix default exists.
- [ ] Run `python -m unittest .gpt-codex.tests.test_consumer_projection.ConsumerProjectionTests` and confirm failures identify missing prefix-default parsing/resolution, not a changed consumer boundary.
- [ ] Implement the two helpers and route audit classification through them; let `build_consumer_inventory` select only exact `paths` entries classified `CONSUMER_REQUIRED`.
- [ ] Add precisely the four allowed defaults and delete only exact non-consumer manifest entries proven covered; record before/after measurements.
- [ ] Run `python -m unittest .gpt-codex.tests.test_consumer_projection` and `python .gpt-codex/scripts/validate_consumer_projection.py --root .`; expect PASS with `0/0/0` and inventory equality.
- [ ] Commit boundary: projection-prefix behavior, tests, and manifest compression only. Reviewer gate: verify longest-prefix determinism, exact consumer authority, and fail-closed unknown behavior.

**Produced authority/evidence:** The bounded prefix-default contract and measured manifest-growth reduction; Task 2 depends on its stable classification of new history files.

## Task 2: Publication Preflight and Immutable Artifact Handoff

**GOAL:** Detect every publication blocker before writes and materialize an immutable W artifact without regeneration.

**SCOPE:** Modify `.gpt-codex/scripts/publication_contract.py`, `.gpt-codex/scripts/release_framework.py`, `.gpt-codex/extensions/skills/framework-release/SKILL.md`, `.gpt-codex/tests/test_publication_authority.py`, and `.gpt-codex/tests/test_release_packaging.py`.

**CONSTRAINTS:** Reuse `release-projection`; preflight consumes facts and never publishes. Required capabilities are `TAG_WRITE`, `RELEASE_WRITE`, and `ASSET_UPLOAD`; missing or partial capabilities block before a write. Check version/source, projection, release record, canonical LF, repository identity, W/main ancestry, tag/release conflicts, mechanism availability, W artifact ref/path, SHA-256, and size. Use binary subprocess output; the canonical source is `<W>:dist/gpt-codex-framework-vX.Y.Z-bootstrap.zip`.

**DONE:** Complete valid facts pass; every stated missing/conflict/mismatch blocks; materialized bytes preserve the Git blob hash and size; no release version changes.

**Files and interfaces:** Preserve `validate_result_authority(...)` and `validate_state_authority(...)`. Add `evaluate_publication_preflight(facts: Mapping[str, Any], required_capabilities: frozenset[str] = frozenset({"TAG_WRITE", "RELEASE_WRITE", "ASSET_UPLOAD"})) -> list[str]` to `publication_contract.py`. Add `materialize_verified_git_artifact(root: Path, revision: str, relative_path: str, expected_sha256: str, expected_size_bytes: int, destination_dir: Path) -> Path` to `release_framework.py`; use `subprocess.run(..., stdout=subprocess.PIPE, stderr=subprocess.PIPE, check=False)` and write bounded temp output. Keep `validate_release_source`, `package_release`, and `main` publication sequencing unchanged.

- [ ] Add RED tests for each missing and partial capability, tag/release conflict, invalid W:path, SHA mismatch, size mismatch, complete facts pass, and exact binary blob materialization.
- [ ] Run the two focused test modules and confirm import/attribute failures for the new interfaces.
- [ ] Implement pure fact validation with explicit error codes and the binary-safe materializer; it reads Git bytes only when the local artifact is absent, verifies hash and length before returning, and never calls `package_release`.
- [ ] Update the release skill: preflight precedes every publication write; W:path is immutable authority; materialize/verify rather than regenerate; reuse structure without copied release facts; derive current Git/remote facts.
- [ ] Run `python -m unittest .gpt-codex.tests.test_publication_authority .gpt-codex.tests.test_release_packaging`; expect PASS.
- [ ] Commit boundary: existing publication contract/release helper, its tests, and release-skill guidance only. Reviewer gate: verify no new release subsystem or write-capable preflight exists.

**Produced authority/evidence:** An explicit blocker set and binary integrity evidence; Task 6 consumes its coverage count.

## Task 3: Separate Verification Execution from Evidence Transport

**GOAL:** Make one complete verification execution reusable for reporting without creating a result platform.

**SCOPE:** Modify `.gpt-codex/builtins/skills/verification/SKILL.md` and `.gpt-codex/tests/test_harness_reduction_gate.py`.

**CONSTRAINTS:** Do not make `execution_telemetry` an authority or introduce a schema. Use existing Evidence/Result Envelope concepts. Retain, when supplied, `command`, tested SHA, exit code, test count, failures, errors, and concise relevant summary; use `UNKNOWN` for unavailable metrics. Rerun only after source/test or material environment change, incomplete execution, or unauthentic evidence—not for lost console capture or report formatting.

**DONE:** The verification skill states the rule unambiguously, a focused management-only regression locks it, and no private reasoning or new result storage is introduced.

**Files and interfaces:** Keep `framework_feedback.evaluate_structure_change(before_capabilities, after_capabilities, evidence) -> str` unchanged. Add a contract assertion method in `HarnessReductionGateTests`, reading the verification Skill bytes like its existing template assertions; no runtime function signature changes.

- [ ] Write a RED test requiring the named retained fields, the four permitted rerun causes, `UNKNOWN`, and the two prohibited transport-only reruns in the verification Skill.
- [ ] Run `python -m unittest .gpt-codex.tests.test_harness_reduction_gate.HarnessReductionGateTests`; expect a missing-contract assertion.
- [ ] Add one bounded “Evidence transport” section to the verification Skill with those exact rules and no new schema/platform terminology.
- [ ] Re-run the focused module; expect PASS.
- [ ] Commit boundary: verification guidance and its management-only contract test. Reviewer gate: verify this is policy reuse, not telemetry or persistence architecture.

**Produced authority/evidence:** A durable, compact reporting rule and test evidence; Task 6 consumes its rerun-policy closure.

## Task 4: Evidence-Gated Worktree Cleanup

**GOAL:** Add a pure, fail-closed KEEP/CLEAN decision without deleting worktrees.

**SCOPE:** Modify `.gpt-codex/scripts/git_continuity.py`, `.gpt-codex/tests/test_git_continuity.py`, and `.gpt-codex/MAINTENANCE.md`.

**CONSTRAINTS:** Stay in `git-continuity`; no database and no deletion function. Active Work Unit means `KEEP`. `CLEAN` requires durable Git authority, sufficient review/integration, and no dirty-only/non-durable data; missing or unknown evidence means `KEEP`. Operations remain `git worktree list`, `git status`, `git worktree remove`, and `git worktree prune`, with no force removal.

**DONE:** All required fact combinations have tests, the function is pure, and maintenance guidance preserves native-Git operational responsibility.

**Files and interfaces:** Keep `SyncSnapshot`, `SyncDecision`, `evaluate_sync_preflight`, and review routing intact. Add `evaluate_worktree_cleanup(active_work_unit: bool | None, durable_git_authority: bool | None, review_or_integration_sufficient: bool | None, has_dirty_only_data: bool | None) -> str`, returning exactly `"KEEP"` or `"CLEAN"`.

- [ ] Add RED cases to `GitContinuityTests` for active, missing durable ref, dirty-only data, unreviewed/unintegrated, all evidence closed, and unknown evidence.
- [ ] Run `python -m unittest .gpt-codex.tests.test_git_continuity.GitContinuityTests`; expect an import failure for `evaluate_worktree_cleanup`.
- [ ] Implement the pure decision with `CLEAN` only for `False, True, True, False`; return `KEEP` for every other tuple.
- [ ] Add the bounded lifecycle and native command prerequisites to `MAINTENANCE.md`.
- [ ] Re-run the focused test module; expect PASS.
- [ ] Commit boundary: decision function, unit tests, and maintenance policy only. Reviewer gate: confirm it cannot call Git or delete a worktree.

**Produced authority/evidence:** Exhaustive safety-decision coverage and a documented operational boundary; Task 5 audits its effects and Task 6 records coverage.

## Task 5: Repository and Context Reduction Audit

**GOAL:** Produce evidence-based recommendations without deleting, merging, or deriving repository contents.

**SCOPE:** Create `docs/superpowers/reviews/2026-09-15-post-v2.7-efficiency-reduction-review.md` only.

**CONSTRAINTS:** Task 1 must already classify `docs/superpowers/**`. Measure the post-Tasks-1–4 tree and classify candidates only as `KEEP`, `MERGE`, `DERIVE_INSTEAD_OF_STORE`, `ARCHIVE`, `CLEAN_AFTER_WORK_UNIT`, or `REMOVE`. `REMOVE`, `MERGE`, and `DERIVE_INSTEAD_OF_STORE` require no active authority or live validator/test/tool dependency, recoverability, durable history where applicable, and capability preservation; incomplete evidence is `KEEP`. No history rewrite or forced deletion; unexpected source-reduction opportunities are recommendations for separate GPT/User adjudication.

**DONE:** The review covers the manifest, DEVELOPMENT_HISTORY footprint, `.superpowers/sdd`, releases metadata, current dist outputs, temporary/worktree lifecycle, and prior Task-8 nine KEEP candidates; it identifies measured reductions already achieved and future-growth reduction.

**Files and interfaces:** Create the named review with measured input commands and cited Task 1–4 evidence. Consume `audit_projection_paths`, `build_consumer_inventory`, `evaluate_worktree_cleanup`, and Task 2 preflight coverage; change none of them.

- [ ] Capture post-Task measurements with `git ls-files`, manifest parsing, `audit_projection_paths`, inventory comparison, and the Task 4 decision matrix.
- [ ] Write the review with an evidence table for every required surface and an explicit status/reason/dependency/recoverability/capability column for each candidate.
- [ ] Classify unproven candidates `KEEP`; write recommendations only for independently authorized future work.
- [ ] Verify this file is automatically `DEVELOPMENT_HISTORY` through Task 1’s default using the projection validator.
- [ ] Commit boundary: the one review artifact only. Reviewer gate: verify no recommendation silently performs a deletion or invalidates prior KEEP authority.

**Produced authority/evidence:** An auditable current-tree assessment used by Task 6; no implementation authority for its recommendations.

## Task 6: Final Capability-Preservation and Efficiency Verification

**GOAL:** Close the integrated implementation with measured proof that capability was retained and at least one cost improved.

**SCOPE:** Modify `.gpt-codex/tests/test_harness_reduction_gate.py` only if a final regression assertion is needed; otherwise make no source changes. Produce the final evidence in the Work Unit Result, not a new schema, platform, or release.

**CONSTRAINTS:** No new architecture, publication, merge, or version work. Verify modules `7`, no Kernel/schema/VERSION change, no eighth module/second release system/worktree database/private reasoning, no rewritten history, and no silent removal of Task-8 KEEP authorities. Cosmetic-only outcome is failure.

**DONE:** All required validators and full discovery pass; measured before/after evidence covers manifest bytes, exact/redundant entries, release-blocker coverage, verification rerun closure, and worktree-decision coverage; at least one measured cost improves.

**Files and interfaces:** Consume Task 1’s `audit_projection_paths` and `build_consumer_inventory`, Task 2’s `evaluate_publication_preflight`, Task 4’s `evaluate_worktree_cleanup`, Task 5 review, and `evaluate_structure_change(...)`. If changed, add one `HarnessReductionGateTests` assertion that required integrated evidence exists; do not add a new function.

- [ ] Write any needed RED assertion for the exact evidence fields and capability-preservation gate, then run its focused test and confirm the missing evidence failure.
- [ ] Collect immutable baseline and post-change measurements, compare consumer inventories byte-for-byte/entry-for-entry, and reject any loss or expansion.
- [ ] Implement only the minimum test assertion needed; do not alter runtime behavior.
- [ ] Run `python .gpt-codex/scripts/validate_consumer_projection.py --root .`, `python .gpt-codex/scripts/validate_framework.py`, `python .gpt-codex/scripts/validate_project.py .`, `python -m unittest discover -s .gpt-codex/tests -p 'test_*.py'`, and `git diff --check`; expect PASS, zero failures, and zero errors.
- [ ] Commit boundary: final gate assertion only if required. Reviewer gate: independently confirm every global constraint and that a real measured reduction—not cosmetic movement—was achieved.

**Produced authority/evidence:** Final implementation review gate; it does not authorize main merge or publication.

## Final Acceptance

Accept only if all six tasks are complete in order; projection is `unknown=0`, `missing_required=0`, and `invalid_classification=0`; consumer inventory is unchanged; Framework and Project validation pass; full unittest discovery has zero failures/errors; `git diff --check` passes; modules remain seven; and measured evidence shows at least one actual cost improvement while `AFTER_CAPABILITIES >= BEFORE_CAPABILITIES`.

## Development Baseline Freeze Requirements

Implementation cannot start at Plan persistence. Required lifecycle:

```text
DESIGN_USER_ACCEPTED -> PLAN_PERSISTED -> PLAN_GPT_REVIEWED -> PLAN_USER_ACCEPTED
-> PROJECTION_CLOSED -> BASELINE_VERIFIED -> DEVELOPMENT_BASELINE_FROZEN
-> Task 1 implementation
```

The freeze records immutable Design `SHA:path`, Plan `SHA:path`, all six task `GOAL`/`SCOPE`/`CONSTRAINTS`/`DONE` blocks, acceptance gates, seven-module boundary, Framework/Kernel/schema versions, projection closure, and clean validation evidence.
