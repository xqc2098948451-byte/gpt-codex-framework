# B7 v2.7.3 Release Work Unit Authority Plan Amendment

**Status: PLAN AMENDMENT CANDIDATE**

> ## THIS PLAN DOES NOT AUTHORIZE EXECUTION.
>
> It creates no release Work Unit, bootstrap object or tag; performs no release
> or publication activity; and does not mutate State, Group C, or Plugin work.
> This candidate only defines the future governed sequence. Each future mutation
> remains subject to its then-current native authority and independently observed
> preconditions.

## 1. Immutable inputs and non-expansion

This Plan implements, without broadening, the accepted B7 Design amendment:

```text
ACCEPTED_DESIGN_COMMIT = 096d326949883eccb42a047f85170ecb7ed97131
ACCEPTED_DESIGN_PATH = docs/superpowers/specs/2026-09-21-b7-v2.7.3-release-work-unit-authority-design-base-binding-correction.md
ACCEPTED_DESIGN_SHA256 = 4961e4d7c13126403d99683b574bfa9b9433fc8cfa0e60677d6ce9ccb1ecacba
CURRENT_MAIN_SHA = 096d326949883eccb42a047f85170ecb7ed97131
CURRENT_STATE_REVISION = 16
CURRENT_RELEASE = 2.7.2+fix.2
TARGET_RELEASE = 2.7.3
```

It supplements, but does not rewrite, the accepted staged-closure Design
`e5fe291c243d0e2bc18e3e5a3cd905e6aa223546:docs/superpowers/specs/2026-09-18-framework-staged-closure-v2.7.2-v2.7.4-design.md`,
its Master Plan
`94eb80869f43800c1c6b1a53d7393b90b4e7433b:docs/superpowers/plans/2026-09-18-framework-staged-closure-v2.7.2-v2.7.4-master-plan.md`,
and the staged-closure authority adjudication
`.gpt-codex/evidence/STAGED-CLOSURE-GROUP-A-BOOTSTRAP-ADJUDICATION.json`.
Those artifacts and the immutable historical Contract Repair semantics remain
authoritative in their existing roles. Nothing here reuses their one-time
authority or creates a new general authority mechanism.

The phases below are defined in this exact order; this candidate executes none
of them:

```text
Phase A — Plan acceptance and immutable ref
Phase B — Freeze canonical release Work Unit bytes
Phase C — Bootstrap authorization object preparation/review/approval
Phase D — One-time Work Unit materialization and integration
Phase E — Native release Work Unit PRE_EXECUTION lifecycle
Phase F — v2.7.3 candidate preparation and verification
Phase G — POST_EXECUTION review / exact USER candidate acceptance
Phase H — governed publication
Phase I — remote verification and separate State/evidence finalization
```

## 2. Phase A — Plan acceptance and immutable ref

This Plan candidate must first complete the following governance lifecycle:

```text
Plan candidate
-> independent CODEX_REVIEWER
-> GPT adjudication
-> exact USER acceptance
-> durable integration
-> independent remote Plan integration verification
-> immutable accepted Plan commit/path ref
```

After durable integration, independently verify that remote `main` resolves
the exact integrated Plan bytes at the exact integrated Plan commit/path and
still resolves the accepted Design correction ref. Only after that remote
verification passes is the accepted Plan ref considered durably established.
Phase B and Phase C are forbidden before this gate passes.

The accepted Plan reference is its exact future commit SHA and the Plan path,
not a branch name, PR comment, summary, or current HEAD. Until it is durably
resolvable together with the accepted Design reference:

```text
BOOTSTRAP_BEFORE_ACCEPTED_PLAN_REF = FORBIDDEN
```

The bootstrap may never create, integrate, approve, or mutate either the
Design or Plan. Its future scope begins only after the two immutable accepted
amendment refs exist.

## 3. Phase B — Freeze canonical release Work Unit bytes

After Phase A, construct but do not yet materialize canonical UTF-8 JSON bytes
for exactly this future Work Unit:

```text
work_unit_id = framework-v2.7.3-release-001
path = .gpt-codex/work-units/framework-v2.7.3-release-001.json
state = AUTHORIZED
basis_state_revision = 16
project_id = PRJ-FRAMEWORK-MANAGEMENT
kernel_version = 2.0.0
schema_version = 1
```

Before bootstrap preparation, observe live State. If its revision is not 16,
the outcome is `RECONCILIATION_REQUIRED`; the accepted Work Unit contract is
never silently rebased.

Its scope is exactly the following nine owned paths, with no exclusions:

```text
OWNED_PATH_COUNT = 9
EXCLUDED_PATHS = []

.gpt-codex/BOOTSTRAP_PROMPT.md
.gpt-codex/CHANGELOG.md
.gpt-codex/README.md
.gpt-codex/builtins/INDEX.json
.gpt-codex/framework-modules/REGISTRY.json
AGENTS.md
VERSION
releases/INDEX.json
releases/records/v2.7.3.json
```

It must not own its own JSON path,
`.gpt-codex/STATE.json`, `.gpt-codex/evidence/**`, `dist/**`, a Group C path,
or a Plugin path. Its selected extensions reuse the current bounded selection
and do not add a Plugin, Built-in, or authority extension:

```json
{
  "skills": ["github-project-continuity"],
  "guardrails": [
    "universal-safety",
    "cross-project-context-binding",
    "github-repository-binding"
  ],
  "fitness": []
}
```

Its actions are exactly `READ`, `TEST`, `VALIDATE`, `REPORT`, and
`MUTATE_APPROVED_SCOPE`. Its forbidden actions are exactly `COMMIT`, `PUSH`,
`PUBLISH`, `AUTHORIZE`, and `SCOPE_EXPANSION`. It authorizes only bounded
release-candidate source/metadata preparation, never candidate packaging
transport, publication, or State finalization.

The frozen acceptance contract is:

```text
TARGET_VERSION = 2.7.3
PREVIOUS_VERSION = 2.7.2+fix.2
TARGET_TAG = v2.7.3
TARGET_ARTIFACT = gpt-codex-framework-v2.7.3-bootstrap.zip
TARGET_RELEASE_RECORD = releases/records/v2.7.3.json
TASKS_5_9_TECHNICAL_SEMANTICS_CHANGED = NO
B6_CLEAN_ROOM_EVIDENCE_REMAINS_VALID = YES
KERNEL_VERSION_CHANGE = FORBIDDEN_UNLESS_SEPARATELY_AUTHORIZED
GROUP_C = NO
PLUGIN = NO
BRIDGE_REUSE = NO
HISTORICAL_REWRITE = NO
PUBLICATION_UNDER_WORK_UNIT = NO
```

The resulting Work Unit must bind immutable artifact references:

```text
artifact_refs.design =
  096d326949883eccb42a047f85170ecb7ed97131:
  docs/superpowers/specs/2026-09-21-b7-v2.7.3-release-work-unit-authority-design-base-binding-correction.md

artifact_refs.plan =
  <future exact accepted Plan commit SHA>:
  docs/superpowers/plans/2026-09-21-b7-v2.7.3-release-work-unit-authority-plan-amendment.md
```

No mutable branch substitutes for either ref. Validate the canonical bytes as
JSON and against the current schema, compute and freeze their SHA-256, and
place that exact hash in the bootstrap authorization object. A byte change
after approval is `RECONCILIATION_REQUIRED`.

## 4. Phase C — Bootstrap authorization object preparation/review/approval

The new external, one-time authorization object is exactly:

```text
BOOTSTRAP_ID = framework-v2.7.3-release-work-unit-bootstrap-001
REMOTE_REF = refs/tags/bootstrap/framework-v2.7.3-release-work-unit-001
EXECUTOR_ROLE = USER_LOCAL
ONE_TIME = true
MUTATION_PATH_COUNT = 1
MUTATION_PATH = .gpt-codex/work-units/framework-v2.7.3-release-001.json
OPERATION = CREATE_ONLY
```

If the target Work Unit path exists, the outcome is
`RECONCILIATION_REQUIRED`; no overwrite is permitted.

Canonical bootstrap-payload formation is forbidden until Phase A's independent
remote Plan integration verification has passed. The required order is exactly:

```text
remote Plan integration verification
-> fresh repository/base/State observation
-> freeze exact expected_base_sha
-> form canonical bootstrap payload
```

Its canonical payload must bind at least:

```text
bootstrap_id = framework-v2.7.3-release-work-unit-bootstrap-001
repository_id = 1366213495
repository_full_name = xqc2098948451-byte/gpt-codex-framework
default_branch = main
expected_base_sha = <fresh live main only after independent remote verification of accepted Plan integration; contains both accepted amendment refs>
expected_state_revision = 16
accepted_design_amendment_ref = 096d326949883eccb42a047f85170ecb7ed97131:docs/superpowers/specs/2026-09-21-b7-v2.7.3-release-work-unit-authority-design-base-binding-correction.md
accepted_plan_amendment_ref = <future exact accepted Plan SHA:path>
release_work_unit_id = framework-v2.7.3-release-001
release_work_unit_path = .gpt-codex/work-units/framework-v2.7.3-release-001.json
release_work_unit_canonical_bytes_sha256 = <future exact hash>
mutation_scope = exact one-path list
operation = CREATE_ONLY
executor_role = USER_LOCAL
one_time = true
```

`expected_base_sha` is freshly observed only after accepted-Plan integration
has independently passed remote verification, then frozen before canonical
bootstrap-payload formation. It must never be hard-coded from this pre-Plan
main. Before object creation, a
read-only independent `PRE_EXECUTION` review must verify the accepted Design
and Plan refs, live repository identity and main SHA, State revision 16,
exact Work Unit bytes/hash, exact bootstrap payload bytes, one-path scope,
`CREATE_ONLY`, one-time semantics, and `USER_LOCAL` executor. Any mismatch
blocks the bootstrap.

Following that review and GPT adjudication, `USER_APPROVER` must approve the
exact canonical bootstrap payload bytes. Approval of a summary, branch, PR
comment, or approximation is insufficient. Record its exact SHA-256; no
repository mutation precedes it.

Create the immutable external object at the exact `REMOTE_REF` using the
existing immutable annotated-tag governance pattern. A preexisting ref,
force update, replacement, or reuse is `RECONCILIATION_REQUIRED` or forbidden
as applicable. Preserve any current signed-annotated-tag signing requirement;
do not weaken signing verification. The tag/object carries the exact approved
payload.

## 5. Phase D — One-time Work Unit materialization and integration

`USER_LOCAL` must verify the immutable bootstrap ref, exact payload
bytes/hash, repository identity, expected main SHA, State revision 16, absent
target path, and canonical Work Unit hash before materializing exactly one
file. Then use current Work Unit and project validators. The complete actual
repository mutation union is only:

```text
.gpt-codex/work-units/framework-v2.7.3-release-001.json
```

No second path is allowed. The bounded integration sequence is:

```text
materialize exact reviewed Work Unit bytes
-> validate
-> independent exact-byte/path verification
-> create exactly one Work Unit candidate commit
-> verify parent/base
-> verify one-path surface
-> push candidate branch only
-> independent remote candidate verification
-> exact USER acceptance if required by the current governance integration route
-> fast-forward exact accepted Work Unit commit to main
-> independently verify main
```

Amend, squash, rebase, extra paths, and a second commit are forbidden. The
bootstrap does not mutate State. Once remote main contains the exact accepted
Work Unit bytes, verify its immutable Work Unit ref and mark the bootstrap
`TERMINATED`:

```text
BOOTSTRAP_REUSE = FORBIDDEN
BOOTSTRAP_SECOND_WORK_UNIT = FORBIDDEN
```

Historical bootstrap evidence is retained, never deleted or rewritten.

## 6. Phase E — Native release Work Unit PRE_EXECUTION lifecycle

Only after bootstrap termination may B7 release mutation start. The release
Work Unit never substitutes for this fresh native entry gate:

```text
EXECUTION_INSTRUCTION
-> independent PRE_EXECUTION review
-> immutable target_work_unit_ref
-> exact nine-path scope
-> expected State revision = 16
-> expected base SHA = current main
```

All current Task-7/native authority checks remain active.

## 7. Phase F — v2.7.3 candidate preparation and verification

Under the native release Work Unit and no other authority, prepare exactly the
nine frozen paths with `VERSION = 2.7.3`. The current owner-derived release
surface remains nine paths unless a real production owner contradiction is
independently discovered and reconciled under separately valid authority.

The generated publication artifacts
`dist/gpt-codex-framework-v2.7.3-bootstrap.zip`,
`dist/gpt-codex-framework-v2.7.3-bootstrap.zip.sha256`, and
`dist/gpt-codex-framework-v2.7.3-release.json` remain generated, untracked
publication artifacts, not tracked candidate paths.

Before POST review, require focused Group-B tests, the full test suite,
framework validator, project validator, consumer-projection validation,
version consistency, release-packaging validation, Task 5–9 composed proof,
`git diff --check`, and actual Git-scope verification. Package success alone
is insufficient.

## 8. Phase G — POST_EXECUTION review / exact USER candidate acceptance

The candidate lifecycle is:

```text
independent POST_EXECUTION review
-> GPT adjudication
-> exact candidate content lock
-> exact USER candidate acceptance
```

Only the exact reviewed and accepted candidate may proceed.

## 9. Phase H — governed publication

Publication remains under existing authority and requires fresh preflight
capabilities `TAG_WRITE`, `RELEASE_WRITE`, and `ASSET_UPLOAD`. It may publish
only the exact accepted candidate with `tag = v2.7.3` and
`artifact = gpt-codex-framework-v2.7.3-bootstrap.zip`. Neither the release
Work Unit nor bootstrap self-publishes.

## 10. Phase I — remote verification and separate State/evidence finalization

Use tool-observed equality for main SHA, tag target, release version, asset
identity, asset SHA-256, asset size, `VERSION`, and release record. Only then
may the publication be classified `REMOTE_ACTIVE`.

State/evidence finalization is a separate native lifecycle. Do not silently
add `.gpt-codex/STATE.json`, publication-verification Evidence, or publication
Result to the nine-path release Work Unit. If finalization authority is absent
at that later gate, explicitly reconcile it rather than widening this Work
Unit.

## 11. Fail-closed prohibitions and recovery

The following shortcuts are forbidden: Group-B or successor-seed scope
expansion; historical revision-12 seed reuse; bridge-003, bridge-004, or
bridge-005 reuse; bridge-006 creation; bootstrap reuse; a generic Work Unit
factory; bootstrap self-authorization; Work Unit self-ownership or
self-authorization; State, release, or publication mutation during bootstrap;
Group C work; and Plugin work.

Apply these fail-closed outcomes without inventing a bypass:

```text
authority/base/State mismatch -> RECONCILIATION_REQUIRED
test/validator failure -> FAILED / retain evidence
capability unavailable -> BLOCKED
candidate bytes changed after review/acceptance -> RECONCILIATION_REQUIRED
bootstrap tag already exists -> RECONCILIATION_REQUIRED
Work Unit path already exists before CREATE_ONLY -> RECONCILIATION_REQUIRED
```

## 12. Plan self-review checklist

```text
accepted Design preserved = YES
authority widened beyond Design = NO
Plan authoring path count = 1
future release Work Unit path count = 9
bootstrap mutation path count = 1
bootstrap State mutation = NO
bootstrap release mutation = NO
bootstrap publication authority = NO
release Work Unit publication authority = NO
remote Plan integration verification before base freeze = YES
legacy bridge reuse = NO
generic bootstrap precedent = NO
Group C authority = NO
Plugin authority = NO
```
