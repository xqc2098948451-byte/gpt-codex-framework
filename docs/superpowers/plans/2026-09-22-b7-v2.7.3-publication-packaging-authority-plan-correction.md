# B7 v2.7.3 Publication Packaging Authority Correction Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: use `superpowers:subagent-driven-development` or `superpowers:executing-plans` task-by-task. This document is a Plan candidate only; it grants no execution authority.

**Goal:** Close only the v2.7.3 source-candidate → immutable canonical publication artifact authority gap while preserving the accepted source candidate and the existing global publication contract.

**Architecture:** `S` remains the immutable USER-accepted source candidate. A separately authorized packaging lifecycle creates `W` from exact accepted Phase-F artifact bytes plus the bounded seven-path packaging surface. `W` is independently reviewed/accepted and later becomes the exact tag target. Publication and State/Evidence finalization remain separately governed.

**Tech Stack:** Python standard library, Git, GitHub, existing Framework validators, consumer-projection machinery, framework-release/publication contracts.

**Spec:** `7984c2ee0c6d137bce4e6d27b8816ab59cdf5d09:docs/superpowers/specs/2026-09-22-b7-v2.7.3-publication-packaging-authority-design-correction.md`

## Global Frozen Facts

| Field | Exact value |
| --- | --- |
| `S` | `d9dd439a6d6ca944a6f07ed1aa0b386b0ac0a0b7` |
| `S_CONTENT_SET_SHA256` | `7862243dc275f3f27d600717d5e2bdc3a1373f9e49eb9bc80346b23df440efe6` |
| `S_SOURCE_PATH_COUNT` | `9` |
| `TARGET_VERSION` | `2.7.3` |
| `STATE_REVISION` | `16` |
| `ACCEPTED_ARTIFACT` | `gpt-codex-framework-v2.7.3-bootstrap.zip` |
| `ACCEPTED_ARTIFACT_SHA256` | `8da1be98f9d7ea18fb3008685331956dfcb2232ac1ccfdf9085dd466a793a39d` |
| `ACCEPTED_ARTIFACT_SIZE_BYTES` | `139774` |
| `PHASE_F_ARTIFACT_DIRECTORY` | `D:\通用开发框架管理\b7-v2.7.3-validation-artifacts-8f9c379591ec422bb1b6790e07b5345c` |

The local directory is a runtime source locator only, not authority. Authority remains the accepted artifact identity and accepted Design. If the exact accepted artifact bytes are unavailable later, the result is `RECONCILIATION_REQUIRED`; never regenerate them.

## Global Invariants

```text
TASKS_5_9_TECHNICAL_SEMANTICS_CHANGED = NO
SOURCE_CANDIDATE_CHANGED = NO
SOURCE_REACCEPTANCE_REQUIRED = NO unless S bytes change
GLOBAL_PUBLICATION_CONTRACT_CHANGED = NO
KERNEL_CHANGE = NO
SCHEMA_CHANGE = NO
GROUP_C_ACTIVITY = NO
PLUGIN_ACTIVITY = NO
BRIDGE_REUSE = NO
OLD_BOOTSTRAP_REUSE = NO
GENERIC_WORK_UNIT_FACTORY = NO
GENERAL_BOOTSTRAP_PRECEDENT = NO
```

`S`'s nine accepted source-path bytes must remain byte-identical through `W`. `S` must be an ancestor of `W`; `git diff S..W` is not a packaging-scope oracle because independently governed intervening artifacts are allowed.

## Review Focus

1. `main` or State changes after accepted Plan integration and before bootstrap payload freeze require `RECONCILIATION_REQUIRED`.
2. A missing accepted Phase-F ZIP, or a ZIP whose hash or size differs, requires `RECONCILIATION_REQUIRED`; no replacement bytes may be regenerated.
3. A packaging candidate changing anything outside the exact seven paths, or changing an `S` source byte, is rejected for reconciliation.
4. An absent canonical `W` artifact ref, tag/release conflict, or unavailable `TAG_WRITE`, `RELEASE_WRITE`, or `ASSET_UPLOAD` is `BLOCKED` or `RECONCILIATION_REQUIRED` before a publication write.
5. A partial tag/release/upload without independent remote equality verification or valid later State/Evidence finalization never becomes `REMOTE_ACTIVE`; retain the last verified lifecycle state.

## File and Authority Boundaries

This Plan candidate changes only `docs/superpowers/plans/2026-09-22-b7-v2.7.3-publication-packaging-authority-plan-correction.md`. Future phases may create the one Work Unit path and later mutate exactly the seven packaging paths, but neither operation is authorized by this Plan candidate.

The frozen seven-path packaging scope is:

1. `.gpt-codex/release/consumer-projection-manifest.json`
2. `dist/gpt-codex-framework-v2.7.1-bootstrap.zip` — delete
3. `dist/gpt-codex-framework-v2.7.1-bootstrap.zip.sha256` — delete
4. `dist/gpt-codex-framework-v2.7.1-release.json` — delete
5. `dist/gpt-codex-framework-v2.7.3-bootstrap.zip` — create
6. `dist/gpt-codex-framework-v2.7.3-bootstrap.zip.sha256` — create
7. `dist/gpt-codex-framework-v2.7.3-release.json` — create

## Phase A — Plan Acceptance / Integration

- [ ] Submit this exact Plan candidate to an independent `CODEX_REVIEWER`.
- [ ] Obtain GPT adjudication, exact USER Plan acceptance, and preserve the complete review history.
- [ ] Integrate the exact accepted Plan to `main` by the permitted route, then independently verify remote `main`.
- [ ] Freeze the later Plan reference as the exact immutable accepted-and-durably-integrated `SHA:path` of this file.

No later authority preparation may start before all Phase A checks complete.

## Phase B — Freeze Exact Packaging Work Unit

Only after Phase A remote integration verification, freshly observe repository identity, `origin/main`, and `STATE.revision`. Require revision `16`; otherwise `RECONCILIATION_REQUIRED`. Construct the canonical Work Unit without mutating the repository:

```json
{
  "kernel_version": "2.0.0",
  "schema_version": 1,
  "project_id": "PRJ-FRAMEWORK-MANAGEMENT",
  "work_unit_id": "framework-v2.7.3-publication-packaging-001",
  "goal": "Materialize and validate the exact accepted v2.7.3 publication packaging surface needed to create immutable W, without changing S, publication authority, State, Group C, Plugin, or Contract Repair semantics.",
  "state": "AUTHORIZED",
  "basis_state_revision": 16,
  "scope": {
    "owned_paths": [
      ".gpt-codex/release/consumer-projection-manifest.json",
      "dist/gpt-codex-framework-v2.7.1-bootstrap.zip",
      "dist/gpt-codex-framework-v2.7.1-bootstrap.zip.sha256",
      "dist/gpt-codex-framework-v2.7.1-release.json",
      "dist/gpt-codex-framework-v2.7.3-bootstrap.zip",
      "dist/gpt-codex-framework-v2.7.3-bootstrap.zip.sha256",
      "dist/gpt-codex-framework-v2.7.3-release.json"
    ],
    "excluded_paths": []
  },
  "permissions": {
    "authorized_actions": ["READ", "TEST", "VALIDATE", "REPORT", "MUTATE_APPROVED_SCOPE"],
    "forbidden_actions": ["COMMIT", "PUSH", "PUBLISH", "AUTHORIZE", "SCOPE_EXPANSION"]
  },
  "selected_extensions": {
    "skills": ["github-project-continuity"],
    "guardrails": ["universal-safety", "cross-project-context-binding", "github-repository-binding"],
    "fitness": []
  }
}
```

The Work Unit acceptance requirements must bind `S`, its content-set hash and nine immutable bytes, target `2.7.3`, accepted ZIP hash/size, the seven paths, no ZIP regeneration, no `releases/INDEX.json` or `releases/records/v2.7.3.json` mutation, no State/Evidence/scripts/tests/schemas mutation, no Group C/Plugin/bridge reuse, and no publication authority. Its immediate-parent-to-packaging-commit diff must be exactly the seven paths.

Set `artifact_refs.design.path` to `docs/superpowers/specs/2026-09-22-b7-v2.7.3-publication-packaging-authority-design-correction.md` and `artifact_refs.design.sha` to `7984c2ee0c6d137bce4e6d27b8816ab59cdf5d09`. Set `artifact_refs.plan.path` to this Plan path and `.sha` to the exact USER-accepted, durably integrated Plan commit observed before byte freeze.

- [ ] Serialize exactly as UTF-8, 2-space JSON, LF endings, and one final LF.
- [ ] Freeze byte count, SHA-256, and Git blob SHA-1 if useful; no repository mutation occurs in Phase B.

## Phase C — Fresh Base + Canonical Bootstrap Payload

Only after frozen Work Unit bytes, freshly observe `origin/main` and State again. Freeze `expected_base_sha` only then; never hardcode the Plan-authoring base. If either expected value differs, stop with `RECONCILIATION_REQUIRED`.

Freeze the exact bootstrap payload bytes and SHA-256 with these bindings:

```yaml
bootstrap_id: framework-v2.7.3-publication-packaging-work-unit-bootstrap-001
repository_id: 1366213495
repository_full_name: xqc2098948451-byte/gpt-codex-framework
default_branch: main
expected_base_sha: <fresh exact post-Design-and-Plan-integration origin/main>
expected_state_revision: 16
accepted_design_correction_ref: 7984c2ee0c6d137bce4e6d27b8816ab59cdf5d09:docs/superpowers/specs/2026-09-22-b7-v2.7.3-publication-packaging-authority-design-correction.md
accepted_plan_correction_ref: <exact accepted Plan SHA:path>
packaging_work_unit_id: framework-v2.7.3-publication-packaging-001
packaging_work_unit_path: .gpt-codex/work-units/framework-v2.7.3-publication-packaging-001.json
packaging_work_unit_canonical_bytes_sha256: <exact Phase-B frozen hash>
source_candidate_sha: d9dd439a6d6ca944a6f07ed1aa0b386b0ac0a0b7
source_candidate_content_set_sha256: 7862243dc275f3f27d600717d5e2bdc3a1373f9e49eb9bc80346b23df440efe6
artifact_sha256: 8da1be98f9d7ea18fb3008685331956dfcb2232ac1ccfdf9085dd466a793a39d
artifact_size_bytes: 139774
mutation_scope: only the packaging Work Unit path
operation: CREATE_ONLY
executor_role: USER_LOCAL
one_time: true
```

After this freeze, rebase, any “latest main” substitution, and payload recomputation after approval are forbidden.

## Phase D — Bootstrap Review / Approval / Object

- [ ] An independent read-only `PRE_EXECUTION` reviewer verifies every payload field, frozen byte/hash, `STATE=16`, exact `expected_base_sha`, and absent target Work Unit path.
- [ ] Obtain GPT adjudication and exact USER approval of that exact bootstrap payload.
- [ ] Create once, without force/replacement/reuse, the immutable annotated tag `bootstrap/framework-v2.7.3-publication-packaging-work-unit-001`.

A preexisting tag is `RECONCILIATION_REQUIRED`. Do not mutate State.

## Phase E — One-Time Packaging Work Unit Materialization

- [ ] `USER_LOCAL` verifies the exact bootstrap object and frozen bytes, then creates only `.gpt-codex/work-units/framework-v2.7.3-publication-packaging-001.json`; no second repository path is permitted.
- [ ] Validate schema/project/framework applicability and independently verify exact bytes and path.
- [ ] Create one candidate commit only: its parent equals frozen bootstrap `expected_base_sha`, it contains one new commit and one changed path, and committed bytes equal frozen canonical bytes.
- [ ] Push candidate branch only; independently verify it remotely; obtain exact USER acceptance if the then-current integration route requires it; fast-forward integrate the exact Work Unit commit without force; independently verify remote `main`.

Then set `PACKAGING_BOOTSTRAP = TERMINATED` and `PACKAGING_BOOTSTRAP_REUSE = FORBIDDEN`; retain the historical bootstrap tag unchanged.

## Phase F — Native Packaging Pre-Execution

Only after Phase E termination, create a native `EXECUTION_INSTRUCTION` binding:

```yaml
target_work_unit: framework-v2.7.3-publication-packaging-001
target_work_unit_ref: <exact immutable integrated Work Unit commit:path>
expected_state_revision: 16
expected_base_sha: <fresh current main>
repository_id: 1366213495
repository_full_name: xqc2098948451-byte/gpt-codex-framework
scope_paths: <exact seven packaging paths>
authorized_actions: [READ, TEST, VALIDATE, REPORT, MUTATE_APPROVED_SCOPE]
forbidden_actions: [COMMIT, PUSH, PUBLISH, AUTHORIZE, SCOPE_EXPANSION]
```

Require an independent `CODEX_REVIEWER` `PRE_EXECUTION` PASS before mutation.

## Phase G — Prepare Exact Seven-Path Packaging Candidate

At the Phase-F reviewed base in a clean implementer worktree, verify before mutation that `S` is an ancestor, all nine `S` source bytes equal `S`, State revision is `16`, current `VERSION` is `2.7.3`, tracked v2.7.1 dist files exist, v2.7.3 dist files do not, and projection-manifest facts match the accepted Design.

From `D:\通用开发框架管理\b7-v2.7.3-validation-artifacts-8f9c379591ec422bb1b6790e07b5345c`, require exactly the accepted ZIP, `.sha256`, and release JSON. Verify ZIP SHA-256 `8da1be98f9d7ea18fb3008685331956dfcb2232ac1ccfdf9085dd466a793a39d`, size `139774`, and ZIP integrity `PASS`. Do not run `release_framework.py` and do not regenerate any artifact.

Validate the sidecar filename and its SHA-256 token exactly. Validate release JSON fields: `framework_version=2.7.3`, `kernel_version=2.0.0`, `schema_version=1`, accepted artifact name/hash, and `validation.framework`, `validation.tests`, `validation.zip_integrity`, and `validation.release_hygiene` all `PASS`. Copy these bytes byte-for-byte only.

Mutate the projection manifest only by changing `framework_version` from `2.7.1` to `2.7.3` and adding the three v2.7.3 `GENERATED_ARTIFACT` classifications. Do not change historical classifications. Delete precisely the v2.7.1 triple; create precisely the v2.7.3 triple. The actual Git mutation union must equal seven paths exactly.

## Phase H — Packaging Validation

Before post-review, run and require PASS:

```powershell
python -B .gpt-codex/scripts/validate_consumer_projection.py --root .
python -B .gpt-codex/scripts/validate_framework.py
python -B .gpt-codex/scripts/validate_project.py .
python -B .gpt-codex/tests/test_version_consistency.py
python -B .gpt-codex/tests/test_release_packaging.py
python -B .gpt-codex/tests/test_publication_authority.py
python -B -m unittest discover -s .gpt-codex/tests -p "test_*.py"
git diff --check
```

Use the production actual-Git scope oracle and require precisely seven paths. Reverify `S` ancestry, all nine source bytes, accepted ZIP hash/size, unchanged release record/index, and no State/Evidence/script/test/schema mutation.

## Phase I — Independent Post Review / Exact Packaging Acceptance

An independent `CODEX_REVIEWER` reviews the complete candidate: seven-path scope, three deletions, exact manifest semantics, ZIP hash/size, sidecar, release JSON, full validation, and unchanged nine `S` source bytes.

Freeze the exact candidate content lock: use raw SHA-256 for each final present path, literal `DELETE` for each deletion, case-insensitive deterministic path ordering, and canonical lines `<path><TAB><sha256-or-DELETE><LF>`. Freeze `PACKAGING_CANDIDATE_CONTENT_SET_SHA256`.

- [ ] Complete POST review and GPT adjudication.
- [ ] Present the USER with `PACKAGING_CANDIDATE_CONTENT_SET_SHA256`, accepted ZIP SHA-256, accepted ZIP size, the exact seven-path scope, and expected pre-`W` base SHA.
- [ ] Obtain exact USER acceptance of that seven-path content lock and separately record:

  ```yaml
  USER_LOCAL_W_CREATION_AUTHORIZATION: APPROVE | REJECT
  USER_LOCAL_W_CANDIDATE_PUSH_AUTHORIZATION: APPROVE | REJECT
  USER_LOCAL_W_MAIN_FAST_FORWARD_AUTHORIZATION: APPROVE_IF_REMOTE_VERIFICATION_PASSES | REJECT
  ```

  These are explicit human-local operation authorizations. They are not `CODEX_IMPLEMENTER` `authorized_actions`, a Work Unit permission expansion, implicit approval, or publication authorization. One USER response may contain all three decisions.

After independent POST_EXECUTION review, GPT adjudication, and exact USER acceptance of the seven-path packaging content lock:

```text
CODEX_IMPLEMENTER_EXECUTION_WINDOW = CLOSED
```

The packaging Work Unit has completed its authorized mutation/validation role. No `CODEX_IMPLEMENTER` Instruction may authorize or perform Git commit, candidate-branch push, main integration, tag, Release, or asset upload. The accepted candidate bytes remain frozen.

## Phase J — USER_LOCAL Immutable W Creation, Transport, and Integration

This phase is not an `EXECUTION_INSTRUCTION` under the packaging Work Unit. It does not add `COMMIT` or `PUSH` to `CODEX_IMPLEMENTER`. It reuses the existing bounded B7 `USER_LOCAL` candidate-integration pattern already used for exact governed artifact integration. It creates no new role, action, authority subsystem, generic integration factory, or bootstrap precedent.

```text
W_CREATION_EXECUTOR = USER_LOCAL
W_CANDIDATE_TRANSPORT_EXECUTOR = USER_LOCAL
W_MAIN_INTEGRATION_EXECUTOR = USER_LOCAL
CODEX_IMPLEMENTER_COMMIT = FORBIDDEN
CODEX_IMPLEMENTER_PUSH = FORBIDDEN
CODEX_REVIEWER_MUTATION = FORBIDDEN
```

Only after the explicit `USER_LOCAL_W_CREATION_AUTHORIZATION = APPROVE` and `USER_LOCAL_W_CANDIDATE_PUSH_AUTHORIZATION = APPROVE`, freshly verify that `origin/main` equals the expected pre-`W` base, State revision remains `16`, the accepted candidate content lock still matches, the scope is exactly the seven packaging paths, all nine `S` source bytes are unchanged, and there is no staged or untracked scope escape.

`USER_LOCAL` may then perform only the exact bounded Git operations needed to stage exactly the seven accepted packaging paths; verify the index content/deletion lock equals the USER-accepted candidate lock; create exactly one `W` commit; verify `W` parent equals the expected pre-`W` base; verify `parent..W` changed-path count is `7`; and verify the committed content lock equals USER acceptance. Editing candidate bytes, amend, rebase, squash, a second commit, extra paths, and force operations are forbidden.

`USER_LOCAL` may then push only `W` to one dedicated `W` candidate branch. The push must be normal and non-force. Verify remote branch SHA equals `W`, `W` parent/base is exact, the seven-path remote surface is exact, the committed content lock is exact, `S` is an ancestor, and the nine `S` source bytes are unchanged. Then require independent remote `W` verification.

Only if independent remote `W` verification is `PASS` and `USER_LOCAL_W_MAIN_FAST_FORWARD_AUTHORIZATION = APPROVE_IF_REMOTE_VERIFICATION_PASSES`, freshly re-observe that `origin/main` still equals the expected pre-`W` base, State revision is still `16`, and the remote candidate branch is still exactly `W`. `USER_LOCAL` may perform only the exact non-force fast-forward `W -> refs/heads/main`; merge commits, squash, rebase, and force are forbidden. Independently verify remote `main = W`, `S` is an ancestor of `W`, the nine `S` source bytes are unchanged, and `W` is the exact seven-path packaging commit.

`CODEX_IMPLEMENTER` and `CODEX_REVIEWER` may assist around this boundary only with `READ`, `TEST`, `VALIDATE`, and `REPORT` as separately validly routed. They are never the executor of `W` commit, candidate push, or main integration.

## Phase K — Publication Preflight

Phase K onward remains separate from the `USER_LOCAL` W integration route. Its approval does not authorize the `v2.7.3` tag, GitHub Release, asset upload, publication, or State/Evidence finalization. Phase L explicit publication authorization remains required. Obtain current remote facts and run production `evaluate_publication_preflight()`. It must return `[]` and prove:

```yaml
source_version: 2.7.3
source_version_closed: true
projection_closed: true
release_record_ready: true
canonical_lf_verified: true
repository_identity_verified: true
work_main_ancestry_verified: true
tag_conflict: false
release_conflict: false
publication_mechanism_available: true
canonical_artifact_ref: <W>:dist/gpt-codex-framework-v2.7.3-bootstrap.zip
artifact_sha256: 8da1be98f9d7ea18fb3008685331956dfcb2232ac1ccfdf9085dd466a793a39d
artifact_size_bytes: 139774
available_capabilities: [TAG_WRITE, RELEASE_WRITE, ASSET_UPLOAD]
```

Materialize the upload candidate from `W` with existing `materialize_verified_git_artifact()` into a fresh no-overwrite temporary destination and verify hash/size again. `USER_PUBLICATION_AUTHORIZATION_REQUIRED = YES`; do not enter publication automatically.

## Phase L — Explicit User Publication Authorization

Present `W` SHA, tag `v2.7.3`, release `v2.7.3`, asset name, SHA-256, size, and canonical artifact ref. Require explicit USER authorization for that exact publication operation. Source- or packaging-candidate acceptance is not a substitute.

## Phase M — Governed Publication

Only after Phase L: create annotated tag `v2.7.3` targeting exactly `W`, push normally without force/update/replacement, create GitHub Release `v2.7.3`, and upload exactly one canonical asset named `gpt-codex-framework-v2.7.3-bootstrap.zip` materialized from immutable `W`. Never regenerate ZIP bytes. Until independent verification, `publication_authority = PUBLICATION_CANDIDATE_ONLY`.

## Phase N — Independent Remote Verification

Independently observe repository identity; remote `main` contains `W`; `S` is ancestor of `W/main`; `v2.7.3` exists; its annotated target is exactly `W`; the GitHub Release is published; asset name/hash/size (`139774`) agree; remote `VERSION=2.7.3`; and `releases/records/v2.7.3.json` is exact and consistent. Use current `verify_remote_activation` semantics. Only exact agreement can classify publication `VERIFIED/PUBLISHED`; do not claim `REMOTE_ACTIVE` yet.

## Phase O — Separate State / Evidence Finalization

The packaging Work Unit does not authorize `.gpt-codex/STATE.json`, publication-verification Evidence, or publication Result. After remote verification, re-observe native finalization authority. Use a separate valid native lifecycle if exact authority exists; otherwise `RECONCILIATION_REQUIRED`. Do not widen packaging scope. Only verified remote publication plus valid durable Result/Evidence and State/continuity finalization may produce `2.7.3 REMOTE_ACTIVE`.

## Failure Rules

| Condition | Required outcome |
| --- | --- |
| authority/base/State mismatch | `RECONCILIATION_REQUIRED` |
| accepted artifact missing/hash mismatch | `RECONCILIATION_REQUIRED` |
| test/validator failure | `FAIL`; no advancement |
| scope escape | `FAIL / ROLE_AUTHORITY_CONFLICT` |
| bootstrap ref preexists | `RECONCILIATION_REQUIRED` |
| CREATE_ONLY Work Unit path preexists | `RECONCILIATION_REQUIRED` |
| USER_LOCAL W authorization absent | `STOP` before commit |
| candidate bytes change after exact acceptance | `RECONCILIATION_REQUIRED` |
| remote `main` changes before W push/integration | `RECONCILIATION_REQUIRED` |
| candidate push or independent remote W verification differs | `STOP`; do not integrate `main` |
| a Codex instruction contains `COMMIT` or `PUSH` | `ROLE_AUTHORITY_CONFLICT` |
| publication capability unavailable | `BLOCKED` |
| tag/release conflict | `RECONCILIATION_REQUIRED` |
| partial publication without verified equality | `PUBLICATION_CANDIDATE_ONLY` |
| missing finalization authority | `RECONCILIATION_REQUIRED` |

## Candidate Self-Review and Validation

- [ ] Confirm every accepted Design section maps to Phases A–O; no unresolved placeholder exists; no generic bootstrap mechanism or scope expansion is invented.
- [ ] Confirm fresh post-Plan `expected_base_sha` freeze, exact Work Unit permissions, `S`-versus-`W` ancestry, exact seven-path `parent..W` scope, explicit publication authorization, and no pre-finalization `REMOTE_ACTIVE` claim.
- [ ] Run:

```powershell
git diff --check
python -B .gpt-codex/scripts/validate_framework.py
python -B .gpt-codex/scripts/validate_project.py .
```

Require PASS for all commands. From `7984c2ee0c6d137bce4e6d27b8816ab59cdf5d09`, the changed-path count must be one and the sole changed path must be this Plan. Commit exactly once as `docs: plan b7 publication packaging authority correction`, push only `governance/b7-publication-packaging-authority-plan-correction`, and do not mutate `main`, Work Units, bootstrap tags, Design, `S`, `dist`, v2.7.3 tag/release, or State.
