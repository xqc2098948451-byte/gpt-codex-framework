# Group C C9 / v2.7.4 Packaging Bootstrap Materialization Reconciliation Design Amendment

**Status:** `DESIGN_AMENDMENT_CANDIDATE_ONLY`. This is Design authoring only. It grants no Plan, Work Unit, bootstrap, Packaging, publication, State, Evidence, commit, push, or main-update authority.

## 1. Bound finding and decision

`ROOT_CAUSE = PACKAGING_BOOTSTRAP_002_EMPTY_ANNOTATION_AFTER_AUTHORIZED_MATERIALIZATION`.

The failure is classified exactly as `AUTHORIZED_MATERIALIZATION_ATTEMPT_WITH_MALFORMED_BOOTSTRAP_OUTPUT`. It is not a source-scope, Packaging-scope, projection-manifest, validation-allocation, source-candidate, State, or publication defect.

`DESIGN_AMENDMENT_REQUIRED = YES`; `PLAN_ONLY_CORRECTION = INSUFFICIENT`.

The sole operational recovery is one exceptional, bounded replacement Packaging authority 003. `OPERATIONAL_REPLACEMENT_003 = EXCEPTIONAL_AND_BOUNDED`; `GENERIC_RETRY_SUBSYSTEM = ABSENT`; `GENERIC_BOOTSTRAP_REISSUE = FORBIDDEN`; `AUTOMATIC_RETRY_AFTER_FAILED_BOOTSTRAP = FORBIDDEN`; and `FUTURE_REUSE_OF_THIS_EXCEPTION = FORBIDDEN`.

## 2. Immutable observed evidence

Remote `main` is `50b4b029c631cc320fb75463e23e9e50d06a9b3d`, State basis is revision `17`, and accepted source candidate `5fd9cb4735d2c21fcefd2e9ce90e280e7e1a2de6` has sole parent `50b4b029c631cc320fb75463e23e9e50d06a9b3d`. Its fixed seven-path content set is `1361` bytes and SHA-256 `3a8c703366510048e2769f4c65e30231062a60f02704a3e759e6aacece73648a`.

Packaging Work Unit 002 authority commit `cdebaf2f0a3aac9c4414ac2e529a9c0367132f4e` has sole parent `5fd9cb4735d2c21fcefd2e9ce90e280e7e1a2de6` and sole changed path `.gpt-codex/work-units/framework-v2.7.4-publication-packaging-002.json`. That file is `6151` bytes, SHA-256 `1257f38b1f590496506d1db59c20aa503c18db5d23fc6b57dd1398ccae30b023`, and Git blob SHA-1 `d9bbc13fe4f8e930e56c0863fe2c03610d1455e9`. Its authority ref resolves to that commit.

Bootstrap tag `refs/tags/bootstrap/framework-v2.7.4-publication-packaging-work-unit-002` is annotated tag object `070cedebe54b3a4d9c59bcc4d61839ff9e0c4ff2`, targets `5fd9cb4735d2c21fcefd2e9ce90e280e7e1a2de6`, and has an empty, zero-byte annotation. It did not materialize the approved `2023`-byte, SHA-256 `9d962a47c977f6d58ca236395a0a4a47efbd4c5c132ae7eb4262366df65abb42` canonical bootstrap payload.

Therefore `BOOTSTRAP_002_CANONICAL_PAYLOAD_MATERIALIZED = NO`, `BOOTSTRAP_002_TARGET_CORRECT = YES`, and `BOOTSTRAP_002_ANNOTATION_CORRECT = NO`.

## 3. Preservation and non-contamination

`PACKAGING_BOOTSTRAP_002_STATUS = FAILED_MATERIALIZATION_EMPTY_ANNOTATION_IMMUTABLE_HISTORICAL_EVIDENCE`. `PACKAGING_WORK_UNIT_002_STATUS = IMMUTABLE_HISTORICAL_EVIDENCE_AUTHORITY_INCOMPLETE`; its authority ref is `IMMUTABLE_HISTORICAL_EVIDENCE_NO_EXECUTION_AUTHORITY`.

Bootstrap 002 and Work Unit 002 must not be deleted, rewritten, force-updated, or reused. Work Unit 002 must not be used for Packaging execution. No cleanup is authorized.

At this amendment's authority baseline, Packaging implementation ref 002 is absent; Packaging execution, Packaging-path mutation, publication, State mutation, and main update are all absent. `RELEASE_CONTENT_SCOPE_REOPEN_REQUIRED = NO`; `SOURCE_SCOPE_REOPEN_REQUIRED = NO`; `PACKAGING_SCOPE_REOPEN_REQUIRED = NO`; and `VALIDATION_MODEL_REOPEN_REQUIRED = NO`.

## 4. Replacement authority and unchanged semantics

The new identities are exactly:

- Work Unit: `framework-v2.7.4-publication-packaging-003` at `.gpt-codex/work-units/framework-v2.7.4-publication-packaging-003.json`.
- Bootstrap: `framework-v2.7.4-publication-packaging-work-unit-bootstrap-003` at `refs/tags/bootstrap/framework-v2.7.4-publication-packaging-work-unit-003`.
- Work Unit authority ref: `refs/heads/governance/group-c-c9-v2.7.4-packaging-work-unit-candidate-003`.
- Packaging implementation ref: `refs/heads/governance/group-c-c9-v2.7.4-packaging-candidate-003`.

No 002 identity is reused. WU003 explicitly supersedes WU002 only for continuation of this v2.7.4 release; it neither mutates nor invalidates historical evidence.

Packaging owns exactly the established nine paths, in their established order: consumer projection manifest; the three v2.7.3 distribution artifacts; the three v2.7.4 distribution artifacts; `releases/INDEX.json`; and `releases/records/v2.7.4.json`. The release-content closure remains seven source paths plus nine Packaging paths, totaling sixteen. No tenth Packaging path and no source path enter Packaging scope.

`PACKAGING_BUSINESS_SEMANTICS_CHANGED = NO`. WU003 preserves the accepted source candidate and content set, `VERSION_PLUS_NEW_ARTIFACT_CLASSIFICATION`, the three new `GENERATED_ARTIFACT` classifications, production canonicalized ZIP comparison, independent canonicalized parity replay, repository projection validation, the 635-test closure expectation, C8 focused suites, and State basis 17.

## 5. Closed descendant topology and authority bases

The only permitted recovery topology is:

```text
main 50b4b029...
  -> accepted source candidate 5fd9cb47...
    -> accepted reconciliation Design commit
      -> accepted reconciliation Plan commit
        -> Packaging WU003 authority commit
          -> Packaging implementation candidate 003
```

The source candidate is neither rewritten nor rebased; existing refs are not force-updated; and main does not advance before final-candidate acceptance. This preserves an eventual non-force main fast-forward to the final candidate.

`SOURCE_CONTENT_BASE = 5fd9cb4735d2c21fcefd2e9ce90e280e7e1a2de6`. In contrast, `FRESH_PACKAGING_AUTHORITY_BASE` is the future accepted reconciliation Plan commit. The WU003 commit parent, bootstrap003 target, and bootstrap003 expected base must equal that future accepted Plan commit. They must not equal malformed WU002 commit `cdebaf2f0a3aac9c4414ac2e529a9c0367132f4e`, and bootstrap002 is not authority.

## 6. Final scope accounting and gates

The eventual actual-diff validator must distinguish `RELEASE_CONTENT_PATHS = 16` from reconciliation governance paths: this Design, its future Plan, and WU003. Those three governance paths are explicitly accounted for and are not release-content scope expansion. No unrelated governance path is authorized.

Before any WU003 freeze, a fresh reconciliation Plan must bind this accepted Design, the immutable facts, all replacement identities, and the governance-path accounting. WU003 and bootstrap003 then require their own canonical-freeze, independent review, GPT adjudication, and exact USER approval. Packaging execution remains separately gated by fresh remote verification, a fresh native instruction, PRE_EXECUTION review, and GPT adjudication.

## 7. Required conclusions

`PACKAGING_SCOPE_CHANGED = NO`; `SOURCE_SCOPE_CHANGED = NO`; `VALIDATION_MODEL_CHANGED = NO`; `PACKAGING_WU002_REUSE = FORBIDDEN`; `PACKAGING_BOOTSTRAP_002_REUSE = FORBIDDEN`; `PACKAGING_BOOTSTRAP_002_REWRITE = FORBIDDEN`; `REPLACEMENT_PACKAGING_AUTHORITY = 003`; `REPLACEMENT_003_NEW_IDENTITIES_REQUIRED = YES`; `MAIN_PRE_FINALIZATION_AUTHORITY = 50b4b029c631cc320fb75463e23e9e50d06a9b3d`; and `STATE_BASIS = 17`.
