# B7 v2.7.3 Release Work Unit Authority Design Base-Binding Correction

**Status: DESIGN CORRECTION AMENDMENT CANDIDATE**

> This candidate is a single-rule Design-authority correction. It does not
> authorize release Work Unit creation, bootstrap authority or execution,
> release/version mutation, State mutation, publication, Group C, or Plugin
> work.

## 1. Immutable predecessor and confirmed contradiction

The immutable predecessor Design is:

```text
PREDECESSOR_DESIGN_REF =
3d2cab9bb9eb5fea4b362a65654d1e5690e5730b:
docs/superpowers/specs/2026-09-21-b7-v2.7.3-release-work-unit-authority-design-amendment.md

PREDECESSOR_DESIGN_SHA256 =
91676669aa91b49bc0681d429648f17f7ae5d79bdc078e08c1d141273c02f9db

CURRENT_MAIN_SHA = 3d2cab9bb9eb5fea4b362a65654d1e5690e5730b
CURRENT_STATE_REVISION = 16
```

The predecessor requires that both accepted amendment artifacts be durably
integrated before Phase 2 bootstrap may begin, while also hard-coding:

```text
expected_base_sha =
d571b398f5a8ce69be34d750e224ab9f6c157739
```

Plan integration necessarily changes `main` after `d571...`; therefore those
two requirements cannot govern one canonical bootstrap payload.

```text
DESIGN_BASE_BINDING_CONTRADICTION_CONFIRMED = YES
```

This is a Design-authority defect only. It is not a Task-5–9 production defect,
release implementation defect, bootstrap execution failure, or State
inconsistency.

## 2. Single superseded semantic rule

Only the predecessor's bootstrap `expected_base_sha` rule is superseded.

```text
OLD RULE
expected_base_sha =
d571b398f5a8ce69be34d750e224ab9f6c157739

CORRECTED RULE
expected_base_sha =
the exact freshly observed origin/main SHA after every required B7
governance artifact has been USER-accepted, durably integrated, and
independently verified, immediately before canonical bootstrap payload
formation.
```

The resulting exact SHA is frozen into the canonical bootstrap payload:

```text
BOOTSTRAP_BASE_IS_DYNAMIC_BEFORE_FREEZE = YES
BOOTSTRAP_BASE_IS_IMMUTABLE_AFTER_PAYLOAD_FREEZE = YES
```

No other scope, role, path, permission, or lifecycle rule changes.

## 3. Required ordering and fresh observation

The required ordering is exactly:

```text
original accepted Design
-> accepted Design correction
-> revised Plan candidate
-> independent Plan review
-> GPT adjudication
-> exact USER Plan acceptance
-> durable Plan integration
-> remote Plan integration verification
-> fresh repository/base/State observation
-> freeze exact expected_base_sha
-> form canonical bootstrap payload
```

Bootstrap preparation before accepted Plan integration remains forbidden. At
canonical bootstrap-payload preparation, independently observe:

```text
NEW_WORK_PREFLIGHT = CLEAN_SYNCED
origin/main = freshly observed exact remote main
local intended bootstrap base = same exact SHA
State revision = 16
expected_base_sha = that exact observed main SHA
```

This correction does not predetermine the SHA. Before `USER_LOCAL` performs
the one-path `CREATE_ONLY` materialization, require:

```text
live origin/main == approved bootstrap.expected_base_sha
current State revision == 16
```

If either equality fails, the outcome is `RECONCILIATION_REQUIRED`. No automatic
recomputation after USER approval, payload rebase, or “latest main”
substitution is permitted.

## 4. State binding is unchanged

This correction changes only the timing of the Git-base binding. It preserves:

```text
expected_state_revision = 16
release Work Unit basis_state_revision = 16
```

If State revision changes before bootstrap execution, the outcome remains
`RECONCILIATION_REQUIRED`.

## 5. Normative successor Design reference

The predecessor remains immutable historical authority. This correction
incorporates every predecessor provision except the single superseded
`expected_base_sha` rule. Once this correction is independently reviewed,
USER-accepted, and durably integrated, its immutable commit/path is the
normative B7 Design reference for future B7 artifacts.

The minimally revised Plan must bind:

```text
accepted_design_ref =
<future immutable accepted correction commit>:
docs/superpowers/specs/2026-09-21-b7-v2.7.3-release-work-unit-authority-design-base-binding-correction.md
```

The future `.gpt-codex/work-units/framework-v2.7.3-release-001.json` must set
`artifact_refs.design` to that immutable accepted correction ref, not the
superseded predecessor alone; the correction preserves the predecessor through
the immutable predecessor reference above. Likewise, the future canonical
bootstrap payload must set `accepted_design_amendment_ref` to the immutable
accepted correction ref, not use the pre-correction Design ref as its sole
Design authority.

## 6. Preserved B7 invariants

All predecessor invariants other than the superseded base-binding rule remain
unchanged:

```text
RELEASE_WORK_UNIT_ID = framework-v2.7.3-release-001
RELEASE_WORK_UNIT_PATH = .gpt-codex/work-units/framework-v2.7.3-release-001.json
RELEASE_WORK_UNIT_PATH_COUNT = 9
BOOTSTRAP_ID = framework-v2.7.3-release-work-unit-bootstrap-001
BOOTSTRAP_MUTATION_PATH_COUNT = 1
BOOTSTRAP_OPERATION = CREATE_ONLY
BOOTSTRAP_EXECUTOR = USER_LOCAL
BOOTSTRAP_ONE_TIME = true
BOOTSTRAP_STATE_MUTATION = NO
BOOTSTRAP_RELEASE_MUTATION = NO
BOOTSTRAP_PUBLICATION_AUTHORITY = NO
WORK_UNIT_SELF_AUTHORIZATION = NO
LEGACY_BRIDGE_REUSE = NO
GROUP_C_AUTHORITY = NO
PLUGIN_AUTHORITY = NO
```

No architecture expands:

```text
NEW_AUTHORITY_SUBSYSTEM = NO
GENERIC_WORK_UNIT_FACTORY = NO
GENERAL_BOOTSTRAP_PRECEDENT_CREATED = NO
NEW_STATE_VALUE = NO
NEW_SCHEMA = NO
NEW_MODULE = NO
DATABASE = NO
QUEUE = NO
DAEMON = NO
SERVICE = NO
```

## 7. Effect on the current Plan candidate

```text
PLAN_CANDIDATE = a06b8d5e061b305ef84bc0c7162ce106c33f1bca
STATUS = REVIEWED_WITH_ACCEPTED_FINDING
```

That Plan is neither accepted nor discarded, and this correction does not
modify it. After this correction becomes an immutable accepted ref, the Plan
must be minimally revised only to bind the corrected Design ref, replace the
conflicting hard-coded Design-base interpretation with the corrected fresh-
observation rule, and preserve all otherwise-passing Plan review findings. A
full Plan rewrite is not required.

## 8. Candidate self-review

```text
original accepted Design modified in place = NO
semantic correction count = 1
hard-coded bootstrap base removed = YES
fresh post-Plan base observation required = YES
base frozen before bootstrap approval = YES
base immutable after approval = YES
State revision rule changed = NO
release Work Unit scope changed = NO
bootstrap scope changed = NO
publication authority changed = NO
legacy bridge rule changed = NO
Group C authority added = NO
Plugin authority added = NO
```
